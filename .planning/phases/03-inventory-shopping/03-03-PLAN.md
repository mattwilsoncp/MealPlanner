---
phase: 03-inventory-shopping
plan: '03'
type: execute
wave: 3
depends_on: [03-01, 03-02]
files_modified:
  - inventory/services/upc_lookup.py
  - inventory/views.py
  - inventory/urls.py
  - templates/inventory/barcode_scan.html
  - inventory/tests/test_barcode_scan.py
autonomous: true
requirements:
  - BAR-01
  - BAR-02
  - BAR-03
  - BAR-04
must_haves:
  truths:
    - "User can open a dedicated barcode scan page and submit a barcode"
    - "Barcode lookup checks household inventory before calling external UPC service"
    - "Unknown barcode triggers UPC lookup and maps API fields to inventory creation"
    - "User can create an inventory item from lookup results in one action"
  artifacts:
    - path: inventory/services/upc_lookup.py
      provides: External UPC lookup client with normalized response schema
    - path: inventory/views.py
      provides: Barcode lookup and create-from-lookup endpoints
    - path: templates/inventory/barcode_scan.html
      provides: Barcode input and result/create interaction
    - path: inventory/tests/test_barcode_scan.py
      provides: Regression tests for local-first lookup and API fallback
  key_links:
    - from: barcode lookup view
      to: InventoryItem.objects.filter(barcode=...)
      via: local-first check
      pattern: filter\(.*barcode
    - from: barcode lookup view
      to: inventory/services/upc_lookup.py
      via: external fallback call
      pattern: lookup_upc
---

<objective>
Implement end-to-end barcode scanning with local-first resolution and UPC fallback.

Purpose: Accelerate inventory entry while avoiding duplicate household items and preserving household ownership.
Output: Dedicated scan page, barcode endpoints, UPC integration service, and tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-inventory-shopping/03-02-PLAN.md
@inventory/views.py
@inventory/models.py
@inventory/urls.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build UPC lookup client and local-first barcode tests</name>
  <files>inventory/services/upc_lookup.py, inventory/tests/test_barcode_scan.py</files>
  <read_first>
    - inventory/models.py
    - inventory/views.py
    - meal_planner_app/tests.py
  </read_first>
  <behavior>
    - Existing household barcode returns local item and does not call external API.
    - Missing local barcode calls UPC client and normalizes fields (name, brand, size, image, category, barcode).
    - UPC failures return structured error payload, not server 500.
  </behavior>
  <action>Create `inventory/services/upc_lookup.py` with `lookup_upc(barcode: str) -> dict` using standard-library HTTP client (`urllib.request`) and timeout handling. Normalize response keys to: `title`, `brand`, `size`, `image_url`, `category`, `barcode`. Add `inventory/tests/test_barcode_scan.py` with mocks for external HTTP to enforce local-first lookup and fallback behavior before implementing view wiring.</action>
  <acceptance_criteria>
    - inventory/services/upc_lookup.py exports `lookup_upc`
    - tests assert local inventory check happens before external call
    - tests cover error handling path for external lookup failure
  </acceptance_criteria>
  <verify>
    <automated>python manage.py test inventory.tests.test_barcode_scan -v 2</automated>
  </verify>
  <done>Barcode lookup contract is test-backed and resilient to upstream UPC failures.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement barcode lookup and create-from-result endpoints</name>
  <files>inventory/views.py, inventory/urls.py</files>
  <read_first>
    - inventory/views.py
    - inventory/urls.py
    - inventory/tests/test_barcode_scan.py
  </read_first>
  <behavior>
    - GET/POST barcode lookup endpoint returns local hit or UPC payload.
    - Create-from-result endpoint persists household inventory item with mapped metadata.
    - Duplicate barcode in same household updates quantity or returns conflict per explicit branch.
  </behavior>
  <action>Add routes `/inventory/barcode/`, `/inventory/api/barcode/lookup/`, and `/inventory/api/barcode/create/`. In lookup handler: query `InventoryItem` by `household` + `barcode` first (BAR-02). If not found, call `lookup_upc` (BAR-03) and return normalized data. In create handler: map UPC response into `InventoryItem` fields (`name`, `barcode`, `category`, `notes`, optional image URL handling strategy) and persist under authenticated household (BAR-04). Keep all mutations CSRF-protected.</action>
  <acceptance_criteria>
    - inventory/urls.py contains barcode page + two barcode API routes
    - lookup endpoint returns explicit source flag (`local` or `upc`)
    - create endpoint writes InventoryItem with household scope and barcode value
  </acceptance_criteria>
  <verify>
    <automated>python manage.py test inventory.tests.test_barcode_scan -v 2 && python manage.py check</automated>
  </verify>
  <done>Barcode workflow can discover and create inventory entries safely.</done>
</task>

<task type="auto">
  <name>Task 3: Build dedicated barcode scan page UI</name>
  <files>templates/inventory/barcode_scan.html</files>
  <read_first>
    - templates/inventory/inventory_list.html
    - templates/base.html
  </read_first>
  <action>Create `barcode_scan.html` with DaisyUI form for barcode input, lookup trigger, result panel, and "Create Item" action. Render clear states: existing local item found, UPC match found, and not found/error. Include links back to inventory list and expiring pages.</action>
  <acceptance_criteria>
    - barcode_scan.html includes barcode input and lookup submit control
    - template includes UI branches for local result, UPC result, and not-found state
    - template includes create action bound to `/inventory/api/barcode/create/`
  </acceptance_criteria>
  <verify>
    <automated>python manage.py check</automated>
  </verify>
  <done>Dedicated scan page satisfies BAR-01 and completes the user-facing barcode flow.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser barcode input → lookup endpoint | Untrusted barcode text enters lookup and DB queries |
| App server → external UPC API | Third-party response crosses network boundary into app logic |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | T | barcode lookup endpoint | mitigate | Validate barcode format/length before local query or external call; reject malformed input with 400 |
| T-03-08 | I | UPC API integration | mitigate | Normalize and whitelist allowed response fields before persistence; ignore unexpected keys |
| T-03-09 | D | UPC network call | mitigate | Use explicit request timeout and graceful fallback error payload to avoid worker hangs |
</threat_model>

<verification>
- `python manage.py test inventory.tests.test_barcode_scan -v 2`
- `python manage.py check`
</verification>

<success_criteria>
- BAR-01 through BAR-04 are fully test-backed and implemented.
- Barcode scans do local household dedupe before any external request.
- UPC-based creation produces valid household-scoped InventoryItem records.
</success_criteria>

<output>
After completion, create `.planning/phases/03-inventory-shopping/03-03-SUMMARY.md`
</output>
