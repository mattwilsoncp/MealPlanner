---
phase: 03-inventory-shopping
plan: '01'
type: execute
wave: 1
depends_on: []
files_modified:
  - household/models.py
  - household/migrations/0002_household_expiring_threshold_days.py
  - inventory/models.py
  - inventory/forms.py
  - inventory/admin.py
  - inventory/migrations/0002_inventoryitem_image_and_more.py
autonomous: true
requirements:
  - INV-06
  - INV-11
must_haves:
  truths:
    - "Inventory records can store full item metadata including barcode and image"
    - "Each household has a configurable expiring-item threshold used by inventory workflows"
  artifacts:
    - path: household/models.py
      provides: Household preference field for expiration threshold days
    - path: inventory/models.py
      provides: InventoryItem fields for full create/edit support
    - path: inventory/forms.py
      provides: Validated form contracts for inventory create/update
  key_links:
    - from: household.models.Household.expiring_threshold_days
      to: inventory expiration views
      via: household-scoped query filters
      pattern: expiring_threshold_days
    - from: inventory.forms.InventoryItemForm
      to: inventory.views create/update handlers
      via: ModelForm binding
      pattern: InventoryItemForm
---

<objective>
Define the persistence and form contracts for Phase 3 inventory features.

Purpose: Establish stable model/form interfaces before building list/filter/scan/shopping workflows.
Output: Household expiration preference, complete InventoryItem schema, and reusable validated forms.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@inventory/models.py
@household/models.py
@recipes/models.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend core schema for inventory metadata and expiration preferences</name>
  <files>household/models.py, inventory/models.py</files>
  <read_first>
    - household/models.py
    - inventory/models.py
    - recipes/models.py
  </read_first>
  <behavior>
    - Household defaults expiring threshold to 7 days and rejects non-positive values.
    - InventoryItem accepts image upload and keeps existing household scoping/index behavior.
    - InventoryItem metadata fields (name, quantity, unit, category, location, expiration_date, notes, barcode) remain queryable for filters and barcode lookups.
  </behavior>
  <action>Add `expiring_threshold_days = models.PositiveIntegerField(default=7)` to `Household`; add validators to enforce values >= 1. In `InventoryItem`, keep existing fields and add `image = models.ImageField(upload_to="inventory/%Y/%m/%d/", blank=True, null=True)`, preserve barcode indexing, and keep household composite indexes (`household+name`, `household+expiration_date`). Do not remove or rename current fields used by cooking reconciliation.</action>
  <acceptance_criteria>
    - household/models.py contains `expiring_threshold_days`
    - inventory/models.py contains `ImageField(upload_to="inventory/%Y/%m/%d/")`
    - inventory/models.py still contains household composite indexes
  </acceptance_criteria>
  <verify>
    <automated>python manage.py makemigrations household inventory --check</automated>
  </verify>
  <done>Schema supports INV-06 metadata and INV-11 threshold preference without breaking existing inventory relations.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create explicit inventory form contracts</name>
  <files>inventory/forms.py</files>
  <read_first>
    - inventory/models.py
    - meal_planner_app/forms.py
    - templates/inventory/inventory_form.html
  </read_first>
  <behavior>
    - Create/update forms enforce non-negative quantity and household-safe editable fields.
    - Form fields expose category/location/unit choices from model constants.
  </behavior>
  <action>Create `InventoryItemForm` and `InventoryQuickAddForm` in `inventory/forms.py` using ModelForm patterns from `meal_planner_app/forms.py`. Include editable fields exactly: `name, quantity, unit, category, location, expiration_date, notes, image, barcode`. Add `clean_quantity()` to reject negative values and keep `quantity=0` valid for used-up inventory items.</action>
  <acceptance_criteria>
    - inventory/forms.py exports `InventoryItemForm`
    - inventory/forms.py exports `InventoryQuickAddForm`
    - form validation includes explicit non-negative quantity check
  </acceptance_criteria>
  <verify>
    <automated>python manage.py check</automated>
  </verify>
  <done>Inventory forms are reusable by create/edit views and quick-add endpoints.</done>
</task>

<task type="auto">
  <name>Task 3: Generate migrations and admin support for new schema</name>
  <files>household/migrations/0002_household_expiring_threshold_days.py, inventory/migrations/0002_inventoryitem_image_and_more.py, inventory/admin.py</files>
  <read_first>
    - inventory/admin.py
    - inventory/models.py
    - household/models.py
  </read_first>
  <action>Create migrations for household and inventory schema updates, then update `inventory/admin.py` to include list filters for `category`, `location`, and `expiration_date` plus search fields `name` and `barcode` so barcode-created items are discoverable in admin debugging workflows.</action>
  <acceptance_criteria>
    - migration files exist for both household and inventory changes
    - inventory/admin.py contains search on `barcode`
    - inventory/admin.py contains list_filter for category/location/expiration_date
  </acceptance_criteria>
  <verify>
    <automated>python manage.py makemigrations && python manage.py migrate --noinput && python manage.py check</automated>
  </verify>
  <done>Migrations apply cleanly and admin can inspect new fields.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → inventory create/update forms | Untrusted user input enters persistent inventory records |
| Browser → household preference updates | User-provided threshold controls expiration logic |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | T | inventory/forms.py | mitigate | Enforce field-level validation (quantity >= 0, bounded text lengths via model field constraints) before save |
| T-03-02 | E | household/models.py | mitigate | Keep household scoping unchanged; only authenticated household records can be written in downstream views |
| T-03-03 | D | inventory/models.py image upload | accept | Large uploads are low-risk in local v1; rely on Django upload handling and keep optional image field |
</threat_model>

<verification>
- `python manage.py makemigrations household inventory --check`
- `python manage.py migrate --noinput`
- `python manage.py check`
</verification>

<success_criteria>
- Household has configurable expiring threshold persisted in DB.
- InventoryItem supports image + barcode + full metadata required by INV-06.
- Forms and migrations are ready for list/filter, quick-add, and barcode workflows.
</success_criteria>

<output>
After completion, create `.planning/phases/03-inventory-shopping/03-01-SUMMARY.md`
</output>
