---
phase: 03-inventory-shopping
plan: '04'
type: execute
wave: 1
depends_on: []
files_modified:
  - shopping/apps.py
  - shopping/models.py
  - shopping/services.py
  - shopping/views.py
  - shopping/urls.py
  - shopping/tests/test_generation.py
  - shopping/migrations/0001_initial.py
  - meal_planner/settings.py
  - meal_planner/urls.py
autonomous: true
requirements:
  - SHOP-01
  - SHOP-02
  - SHOP-03
  - SHOP-04
  - MATCH-01
  - MATCH-02
must_haves:
  truths:
    - "Shopping list is persisted per week and auto-loads current week"
    - "If no list exists for week, system generates one from meal plan ingredients"
    - "User can regenerate a week's shopping list on demand"
    - "Generation logic computes ingredient/inventory match metrics (available, total, percentage)"
  artifacts:
    - path: shopping/models.py
      provides: Week and item persistence for shopping lists
    - path: shopping/services.py
      provides: Meal-plan-to-shopping generation and match metric computation
    - path: shopping/views.py
      provides: Week load + auto-generate + regenerate endpoints
    - path: shopping/tests/test_generation.py
      provides: Tests for generation and match math
  key_links:
    - from: shopping/services.py
      to: meal_planner_app.models.MealPlan
      via: week range query
      pattern: MealPlan.objects.filter
    - from: shopping/services.py
      to: ingredients.models.IngredientLink + inventory.models.InventoryItem
      via: quantity matching logic
      pattern: match_percentage
---

<objective>
Create the shopping list subsystem and generator, including match-metric foundations.

Purpose: Establish week-based shopping persistence and automated generation from planned meals and on-hand inventory.
Output: New shopping app wired into project with tested generation and regeneration behavior.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@meal_planner_app/models.py
@ingredients/models.py
@inventory/models.py
@meal_planner/settings.py
@meal_planner/urls.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scaffold shopping app with week/item data model and project wiring</name>
  <files>shopping/apps.py, shopping/models.py, shopping/migrations/0001_initial.py, meal_planner/settings.py, meal_planner/urls.py</files>
  <read_first>
    - meal_planner/settings.py
    - meal_planner/urls.py
    - inventory/models.py
  </read_first>
  <behavior>
    - Shopping list data is keyed by household + week start date.
    - Shopping items are linked to their week record and track checked state.
  </behavior>
  <action>Create `shopping` app with models: `ShoppingListWeek(household, week_start, created_at, updated_at)` and `ShoppingListItem(shopping_week, name, quantity, unit, category, checked, source_recipe, notes)`. Add unique constraint on `(household, week_start)` and indexes for week lookup. Register app in `INSTALLED_APPS` and include `path("shopping/", include("shopping.urls", namespace="shopping"))` in project URLs.</action>
  <acceptance_criteria>
    - shopping/models.py defines both ShoppingListWeek and ShoppingListItem
    - ShoppingListWeek enforces unique household+week_start
    - meal_planner/settings.py includes `shopping`
    - meal_planner/urls.py includes shopping namespace route
  </acceptance_criteria>
  <verify>
    <automated>python manage.py makemigrations shopping && python manage.py migrate --noinput</automated>
  </verify>
  <done>Shopping data layer is persisted and app is wired into Django.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement generation service with ingredient/inventory match metrics</name>
  <files>shopping/services.py, shopping/tests/test_generation.py</files>
  <read_first>
    - meal_planner_app/models.py
    - ingredients/models.py
    - inventory/models.py
    - shopping/models.py
  </read_first>
  <behavior>
    - Generator builds current-week list from meal plans and recipe ingredient links.
    - Existing inventory reduces needed quantities where possible.
    - Service computes per-meal `available_count`, `total_count`, and `match_percentage`.
  </behavior>
  <action>Create `generate_week_shopping_list(household, week_start, regenerate=False)` in `shopping/services.py`. Pull `MealPlan` entries for Monday-Sunday week range, resolve recipe ingredient links, aggregate missing amounts into shopping items grouped by ingredient name/category, and write/update `ShoppingListWeek` + `ShoppingListItem`. In same service expose `compute_meal_match(recipe, inventory_items)` returning `{available_count, total_count, match_percentage}` to satisfy MATCH-01/MATCH-02 foundation. Add tests covering generation on empty week, regenerate behavior, and match percentage calculations.</action>
  <acceptance_criteria>
    - shopping/services.py exports `generate_week_shopping_list`
    - shopping/services.py exports `compute_meal_match`
    - tests verify `match_percentage = available_count / total_count * 100`
  </acceptance_criteria>
  <verify>
    <automated>python manage.py test shopping.tests.test_generation -v 2</automated>
  </verify>
  <done>Generator and match math are deterministic and test-covered.</done>
</task>

<task type="auto">
  <name>Task 3: Add week load and regenerate views</name>
  <files>shopping/views.py, shopping/urls.py</files>
  <read_first>
    - shopping/models.py
    - shopping/services.py
    - meal_planner_app/views.py
  </read_first>
  <action>Create `ShoppingWeekView` that defaults to current week (Monday), loads existing `ShoppingListWeek`, and auto-calls `generate_week_shopping_list` when missing (SHOP-02/SHOP-03). Add `RegenerateShoppingWeekView` POST route to force regeneration (SHOP-04). Include week navigation params (`?week_start=YYYY-MM-DD`) and keep all queries household-scoped.</action>
  <acceptance_criteria>
    - shopping/urls.py contains week page and regenerate POST route
    - ShoppingWeekView auto-generates when week record is absent
    - Regenerate view forces rebuild for requested week
  </acceptance_criteria>
  <verify>
    <automated>python manage.py test shopping.tests.test_generation -v 2 && python manage.py check</automated>
  </verify>
  <done>Week-based shopping list loading/regeneration workflow is complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Planner/recipe/inventory DB → shopping generator | Data from multiple subsystems is combined into persistent shopping state |
| Browser regenerate action → shopping week mutation | Untrusted POST can trigger destructive rebuild |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-10 | T | regenerate endpoint | mitigate | Require POST + authenticated user and re-resolve `ShoppingListWeek` via household filter before mutation |
| T-03-11 | R | shopping/services.py writes | mitigate | Add deterministic generation tests and include source metadata (`source_recipe`) for traceability |
| T-03-12 | E | week query parameter handling | mitigate | Parse week_start safely and default to current Monday on invalid input |
</threat_model>

<verification>
- `python manage.py makemigrations shopping && python manage.py migrate --noinput`
- `python manage.py test shopping.tests.test_generation -v 2`
- `python manage.py check`
</verification>

<success_criteria>
- SHOP-01 through SHOP-04 are delivered via week-based persistence and generation endpoints.
- MATCH-01 and MATCH-02 computation exists in reusable service code.
- Shopping app is wired and migration-backed.
</success_criteria>

<output>
After completion, create `.planning/phases/03-inventory-shopping/03-04-SUMMARY.md`
</output>
