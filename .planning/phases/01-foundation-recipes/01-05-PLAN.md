---
phase: 01-foundation-recipes
plan: '05'
type: execute
wave: 2
depends_on:
  - 01-03
files_modified:
  - reviews/views.py
  - reviews/urls.py
  - reviews/templates/review_queue.html
  - recipes/views.py
autonomous: true
requirements:
  - REV-01
  - REV-02
  - REV-03
  - REV-04
  - REV-05
  - REV-06
  - REV-07
user_setup: []
must_haves:
  truths:
    - Recipes flagged needs_review appear in review queue
    - Review page shows recipe ingredients vs inventory matching
    - User can reconcile ingredients with inventory via dropdown
    - User can quick-add inventory items inline during reconciliation
    - User can save ingredient-to-inventory links
    - User can mark recipe as ready (needs_review=False)
    - User can mark recipe ready directly without reconciliation
  artifacts:
    - path: reviews/views.py
      provides: ReviewQueueView, RecipeReconcileView
    - path: reviews/templates/review_queue.html
      provides: Review queue page UI
    - path: reviews/templates/reconcile.html
      provides: Ingredient reconciliation page UI
  key_links:
    - from: reviews/reconcile
      to: recipes.Recipe
      via: URL param
      pattern: recipe_id
    - from: IngredientLink.inventory_item
      to: inventory.InventoryItem
      via: FK
      pattern: inventory_item
---

<objective>
Create recipe review workflow with ingredient-to-inventory reconciliation.

Purpose: Allow users to maintain recipe quality by flagging recipes for review, reconciling ingredients with inventory, and marking recipes ready.

Output: Review queue page showing needs_review recipes, reconciliation UI to link ingredients to inventory, mark ready functionality.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/research/PITFALLS.md

**From Plans 01-04:**
- Recipe model exists (needs_review flag)
- IngredientLink model exists (inventory_item FK)
- Household model exists
- Templates exist with Tailwind/DaisyUI
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create review queue view</name>
  <files>reviews/views.py, reviews/urls.py</files>
  <read_first>
    - recipes/models.py
  </read_first>
  <action>
    1. Create reviews/views.py:
       - from django.contrib.auth.mixins import LoginRequiredMixin
       - from django.views.generic import ListView, DetailView, UpdateView
       - from django.urls import reverse_lazy
       - from django import forms
       - from recipes.models import Recipe
       
       - class ReviewQueueView(LoginRequiredMixin, ListView):
         - model = Recipe
         - template_name = 'reviews/review_queue.html'
         - context_object_name = 'recipes'
         - def get_queryset(self): return Recipe.objects.filter(household=self.request.user.household, needs_review=True).prefetch_related('ingredients__ingredient', 'ingredients__inventory_item')
       
       - class MarkReadyView(LoginRequiredMixin, UpdateView):
         - model = Recipe
         - fields = []
         - template_name = 'reviews/mark_ready.html'
         - def get_success_url(self): return reverse_lazy('review_queue')
         - def form_valid(self, form): form.instance.needs_review = False; return super().form_valid(form)
         - def get_queryset(self): return Recipe.objects.filter(household=self.request.user.household, needs_review=True)
       
       - class RecipeReconcileView(LoginRequiredMixin, DetailView):
         - model = Recipe
         - template_name = 'reviews/reconcile.html'
         - def get_queryset(self): return Recipe.objects.filter(household=self.request.user.household, needs_review=True)
  </action>
  <acceptance_criteria>
    - ReviewQueueView returns only needs_review=True recipes
    - MarkReadyView sets needs_review=False
    - RecipeReconcileView shows recipe detail
  </acceptance_criteria>
  <verify>
    <automated>grep -q "needs_review=True" reviews/views.py && echo "PASS"</automated>
  </verify>
  <done>Review queue views created</done>
</task>

<task type="auto">
  <name>Task 2: Create URL patterns for reviews</name>
  <files>reviews/urls.py</files>
  <action>
    1. Create reviews/urls.py:
       - from django.urls import path
       - from . import views
       
       - urlpatterns = [
           - path('', views.ReviewQueueView.as_view(), name='review_queue'),
           - path('<int:pk>/ready/', views.MarkReadyView.as_view(), name='mark_ready'),
           - path('<int:pk>/reconcile/', views.RecipeReconcileView.as_view(), name='recipe_reconcile'),
         ]
  </action>
  <acceptance_criteria>
    - reviews/urls.py has review_queue, mark_ready, recipe_reconcile routes
  </acceptance_criteria>
  <verify>
    <automated>grep -q "review_queue" reviews/urls.py && echo "PASS"</automated>
  </verify>
  <done>Review URLs created</done>
</task>

<task type="auto">
  <name>Task 3: Create review queue template</name>
  <files>templates/reviews/review_queue.html, templates/reviews/mark_ready.html</files>
  <action>
    1. Create templates/reviews/review_queue.html:
       - Extends base.html
       - List of recipes needing review
       - Each recipe shows: title, ingredient count, linked inventory count
       - Actions: "Reconcile Ingredients" button, "Mark Ready" button
    
    2. Create templates/reviews/mark_ready.html:
       - Confirmation form: "Mark this recipe as ready?"
       - "Yes, Mark Ready" button
       - "No, Go Back" link
  </action>
  <acceptance_criteria>
    - review_queue.html lists needs_review recipes
    - mark_ready.html has confirmation form
  </acceptance_criteria>
  <verify>
    <automated>ls templates/reviews/review_queue.html templates/reviews/mark_ready.html 2>/dev/null | wc -l</automated>
  </verify>
  <done>Review queue templates created</done>
</task>

<task type="auto">
  <name>Task 4: Create ingredient reconciliation view</name>
  <files>reviews/views.py, templates/reviews/reconcile.html</files>
  <read_first>
    - ingredients/models.py (IngredientLink)
  </read_first>
  <action>
    1. Add reconcile view to reviews/views.py:
       - class SaveReconciliationView(LoginRequiredMixin, UpdateView):
         - model = Recipe
         - fields = []
         - template_name = 'reviews/reconcile.html'
         
         - def get_context_data(self, **kwargs):
           - Get all IngredientLinks for this recipe
           - Get all inventory items for household
           - For each ingredient, provide dropdown of matching inventory items
         
         - def post(self, request, *args, **kwargs):
           - Loop through ingredient_LINK_ID and inventory_ITEM_ID in POST data
           - Update IngredientLink.inventory_item = inventory_item_id
           - Return success or redirect
  </action>
  <acceptance_criteria>
    - SaveReconciliationView handles POST to update inventory links
  </acceptance_criteria>
  <verify>
    <automated>grep -q "inventory_item" reviews/views.py && echo "PASS"</automated>
  </verify>
  <done>Reconciliation view created</done>
</task>

<task type="auto">
  <name>Task 5: Create reconcile template with inventory matching</name>
  <files>templates/reviews/reconcile.html</files>
  <action>
    1. Create templates/reviews/reconcile.html:
       - Recipe title and description
       - List of ingredients with:
         - Ingredient name, quantity, unit
         - Dropdown of matching inventory items (from household inventory)
         - "Quick Add" button to create new inventory item inline
         - Option "Not in Inventory" / "Not Found"
       - "Save Links" button
       - "Mark Ready" button (side by side)
       
    2. Use Alpine.js for:
       - Dynamic dropdown population
       - Quick-add inventory modal
  </action>
  <acceptance_criteria>
    - reconcile.html shows ingredient list
    - Each ingredient has inventory dropdown
    - Quick-add button present
    - Save and Mark Ready buttons present
  </acceptance_criteria>
  <verify>
    <automated>grep -q "inventory_item" templates/reviews/reconcile.html && echo "PASS"</automated>
  </verify>
  <done>Reconcile template created</done>
</task>

<task type="auto">
  <name>Task 6: Wire reviews URLs and update navigation</name>
  <files>meal_planner/urls.py, templates/base.html</files>
  <read_first>
    - meal_planner/urls.py
    - templates/base.html
  </read_first>
  <action>
    1. Add to meal_planner/urls.py:
       - path('reviews/', include('reviews.urls'))
    
    2. Update templates/base.html nav:
       - Add "Reviews" link pointing to review_queue
       - Show badge with count if needs_review > 0
  </action>
  <acceptance_criteria>
    - reviews URLs wired in main urls
    - Nav shows Reviews link
  </acceptance_criteria>
  <verify>
    <automated>grep -q "reviews" meal_planner/urls.py && grep -q "Reviews" templates/base.html && echo "PASS"</automated>
  </verify>
  <done>Reviews wired in navigation</done>
</task>

</tasks>

<verification>
- Navigate to /reviews/ shows list of needs_review recipes
- Click "Reconcile" on recipe shows ingredient list
- Each ingredient shows inventory dropdown with household items
- Selecting inventory item and saving updates IngredientLink
- Click "Quick Add" opens modal to add inventory item
- "Mark Ready" button sets needs_review=False and redirects to queue
- Recipes without reconciliation can still be marked ready directly
</verification>

<success_criteria>
Review queue shows needs_review recipes, reconcile page links ingredients to inventory with dropdown, quick-add works, links save properly, mark ready works with or without reconciliation.
</success_criteria>

<output>
After completion, create .planning/phases/01-foundation-recipes/01-05-SUMMARY.md
</output>
