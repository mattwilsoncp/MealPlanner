---
phase: 01-foundation-recipes
plan: '06'
type: execute
wave: 2
depends_on:
  - 01-02
files_modified:
  - templates/base.html
  - recipes/views.py
  - ingredients/views.py
  - tags/views.py
  - ratings/views.py
autonomous: true
requirements:
  - UI-01
  - UI-02
  - UI-03
  - UI-04
  - UI-05
  - UI-06
user_setup: []
must_haves:
  truths:
    - Tailwind CSS + DaisyUI styling throughout
    - Card-based layouts
    - Server-rendered templates with selective JS enhancement
    - Responsive design (desktop-first with mobile-aware classes)
    - Modal behavior for interactions
    - JSON endpoints for in-page updates
  artifacts:
    - path: templates/base.html
      provides: Base template with responsive nav, Tailwind, DaisyUI, Alpine.js
    - path: recipes/api.py
      provides: JSON endpoints
    - path: templates/includes/modal.html
      provides: Reusable modal component
  key_links:
    - from: Alpine.js x-data
      to: JSON endpoints
      via: fetch
      pattern: /api/.*
---

<objective>
Complete UI/UX foundation with responsive design, modals, and JSON endpoints.

Purpose: Finalize UI polish with Tailwind/DaisyUI, responsive navigation, modal infrastructure, and JSON endpoints for in-page interactions.

Output: Complete UI with responsive design, modals working, JSON APIs available.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

**From Plans 01-05:**
- All templates exist but may need polish
- Tailwind/DaisyUI via CDN currently
- Base template exists
- No JSON endpoints yet
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance base template with responsive nav</name>
  <files>templates/base.html</files>
  <read_first>
    - templates/base.html
  </read_first>
  <action>
    1. Update templates/base.html:
       - Add responsive navbar with hamburger menu for mobile
       - Nav items: Home, Recipes, Review Queue, (Inventory - future)
       - User dropdown: username, logout
       - Use DaisyUI navbar component with responsive classes
       - Add mobile breakpoints: hidden lg:block vs block lg:hidden
       
    2. Add footer with app name and year
    3. Ensure all common elements use DaisyUI classes consistently
  </action>
  <acceptance_criteria>
    - Base template has responsive navbar
    - Mobile hamburger menu works (Alpine.js toggle)
    - Nav shows on desktop, collapses on mobile
  </acceptance_criteria>
  <verify>
    <automated>grep -q "navbar" templates/base.html && grep -q "lg:" templates/base.html && echo "PASS"</automated>
  </verify>
  <done>Responsive navbar implemented</done>
</task>

<task type="auto">
  <name>Task 2: Create reusable modal component</name>
  <files>templates/includes/modal.html</files>
  <action>
    1. Create templates/includes/modal.html:
       - DaisyUI modal structure
       - Parameters: id, title, content (or use {% include %})
       - Close on backdrop click
       - Close on Escape key (Alpine.js)
       
    2. Create JavaScript snippet for modal open/close functions
  </action>
  <acceptance_criteria>
    - modal.html exists with DaisyUI modal
    - Can be included with {% include 'includes/modal.html' with modal_id='xyz' %}
  </acceptance_criteria>
  <verify>
    <automated>ls templates/includes/modal.html 2>/dev/null && echo "PASS"</automated>
  </verify>
  <done>Modal component created</done>
</task>

<task type="auto">
  <name>Task 3: Create JSON API endpoints</name>
  <files>recipes/api.py, recipes/urls.py</files>
  <action>
    1. Create recipes/api.py:
       - from django.http import JsonResponse
       - from django.views.decorators.http import require_http_methods
       - from django.contrib.auth.decorators import login_required
       - from recipes.models import Recipe
       
       - @login_required
       @require_http_methods(["GET"])
       def recipe_list_api(request):
         - Return JSON: [{id, title, description, photo_url, rating, tags: [...]}]
       
       - @login_required
       @require_http_methods(["POST"])
       def recipe_toggle_review(request, pk):
         - Toggle needs_review boolean
         - Return JSON: {success, needs_review}
       
       - @login_required
       @require_http_methods(["GET"])
       def recipe_search_api(request):
         - q param for search
         - Return JSON results
  </action>
  <acceptance_criteria>
    - recipe_list_api returns JSON list of recipes
    - recipe_toggle_review handles POST to toggle needs_review
  </acceptance_criteria>
  <verify>
    <automated>grep -q "JsonResponse" recipes/api.py && echo "PASS"</automated>
  </verify>
  <done>JSON API endpoints created</done>
</task>

<task type="auto">
  <name>Task 4: Wire API endpoints and add inline edit UI</name>
  <files>recipes/urls.py, templates/recipes/recipe_list.html</files>
  <read_first>
    - recipes/urls.py
  </read_first>
  <action>
    1. Add API routes to recipes/urls.py:
       - path('api/list/', views.recipe_list_api, name='recipe_list_api'),
       - path('api/<int:pk>/toggle-review/', views.recipe_toggle_review, name='recipe_toggle_review'),
       - path('api/search/', views.recipe_search_api, name='recipe_search_api'),
    
    2. Update recipe_list.html:
       - Add Alpine.js x-data for inline interactions
       - Add "Toggle Review" button that calls API
       - Add loading states
       - Use fetch() for in-page updates
  </action>
  <acceptance_criteria>
    - API routes respond at /recipes/api/list/ etc.
    - Recipe list uses Alpine.js for interactions
  </acceptance_criteria>
  <verify>
    <automated>grep -q "api" recipes/urls.py && grep -q "x-data" templates/recipes/recipe_list.html && echo "PASS"</automated>
  </verify>
  <done>API wired and inline UI added</done>
</task>

<task type="auto">
  <name>Task 5: Add JSON endpoints for other models</name>
  <files>ingredients/api.py, tags/api.py, ratings/api.py</files>
  <action>
    1. Create ingredients/api.py:
       - ingredient_list_api: list ingredients for household
       - ingredient_search_api: search by name
       
    2. Create tags/api.py:
       - tag_list_api: list tags for household
       - tag_create_api: create new tag
       
    3. Create ratings/api.py:
       - rating_create_api: create/update rating (upsert)
       - rating_get_api: get rating for recipe
  </action>
  <acceptance_criteria>
    - All three API modules exist with working endpoints
  </acceptance_criteria>
  <verify>
    <automated>ls ingredients/api.py tags/api.py ratings/api.py 2>/dev/null | wc -l</automated>
  </verify>
  <done>Additional JSON endpoints created</done>
</task>

<task type="auto">
  <name>Task 6: Final UI polish - cards, forms, buttons</name>
  <files>templates/recipes/*.html, templates/accounts/*.html</files>
  <action>
    1. Update all templates:
       - Use DaisyUI btn classes (btn-primary, btn-secondary, btn-ghost, btn-error)
       - Use DaisyUI card classes (card, card-body, card-title)
       - Use DaisyUI form classes (form-control, label, input, select, textarea)
       - Use DaisyUI alert/notification classes
       - Use DaisyUI badge classes for tags
       - Use DaisyUI table classes for lists
       
    2. Ensure responsive grid: grid-cols-1 md:grid-cols-2 lg:grid-cols-3
    3. Add hover states: hover:bg-* classes
    4. Add transitions where appropriate
  </action>
  <acceptance_criteria>
    - All templates use DaisyUI component classes
    - Cards use card component
    - Forms use form-control
    - Buttons use btn
  </acceptance_criteria>
  <verify>
    <automated>grep -q "btn-" templates/recipes/*.html | wc -l && grep -q "card " templates/recipes/*.html | wc -l</automated>
  </verify>
  <done>UI polished with DaisyUI components</done>
</task>

</tasks>

<verification>
- Navbar responsive - shows links on desktop, hamburger on mobile
- Modal includes work for quick-add inventory, rating, tag creation
- Recipe list uses fetch to toggle review status without page reload
- All forms styled with DaisyUI
- All buttons use DaisyUI btn classes
- Cards use DaisyUI card component
- JSON endpoints return proper responses
</verification>

<success_criteria>
Tailwind/DaisyUI styling complete, responsive navbar works, modals functional, JSON endpoints provide in-page updates, all UI uses DaisyUI components consistently.
</success_criteria>

<output>
After completion, create .planning/phases/01-foundation-recipes/01-06-SUMMARY.md
</output>