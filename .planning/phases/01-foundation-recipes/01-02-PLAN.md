---
phase: 01-foundation-recipes
plan: '02'
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - recipes/models.py
  - recipes/admin.py
  - recipes/views.py
  - recipes/forms.py
  - recipes/urls.py
autonomous: true
requirements:
  - REC-01
  - REC-02
  - REC-03
  - REC-04
  - REC-08
  - REC-09
  - REC-10
  - UI-01
  UI-02
  - UI-03
  UI-04
user_setup: []
must_haves:
  truths:
    - User can create a recipe with title, description
    - User can upload a photo for a recipe
    - User can add optional video URL
    - User can add custom flags (on_hand_idea, leftover_worthy, needs_review)
    - User can view individual recipe details
    - User can edit a recipe
    - User can delete a recipe with confirmation
  artifacts:
    - path: recipes/models.py
      provides: Recipe model with all core fields
    - path: recipes/views.py
      provides: Recipe CRUD views
    - path: recipes/forms.py
      provides: RecipeForm for create/edit
    - path: recipes/admin.py
      provides: Admin registration
  key_links:
    - from: Recipe.household
      to: Household.id
      via: ForeignKey
      pattern: household = models.ForeignKey
---

<objective>
Create core Recipe model and CRUD functionality.

Purpose: Establish the fundamental recipe data model with all required fields and implement create, read, update, delete operations.

Output: Working recipe management with title, description, photo, video URL, custom flags.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md

**Key Pitfall Prevention:**
- Normalize ingredients from day one (separate model, not text field)
- Add indexes on recipe title, tags, rating, created_at

**From Plan 01:**
- Household model exists at household/models.py
- CustomUser has household FK
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Recipe model with all core fields</name>
  <files>recipes/models.py</files>
  <read_first>
    - household/models.py
  </read_first>
  <action>
    1. Create recipes/models.py with:
       - from django.db import models
       - from household.models import Household
       
       - class Recipe(models.Model):
         - household = models.ForeignKey(Household, on_delete=models.CASCADE, related_name='recipes')
         - title = models.CharField(max_length=200, db_index=True)
         - description = models.TextField(blank=True)
         - photo = models.ImageField(upload_to='recipes/%Y/%m/%d/', blank=True, null=True)
         - video_url = models.URLField(blank=True)
         - on_hand_idea = models.BooleanField(default=False, db_index=True)
         - leftover_worthy = models.BooleanField(default=False, db_index=True)
         - needs_review = models.BooleanField(default=True, db_index=True)
         - created_at = models.DateTimeField(auto_now_add=True, db_index=True)
         - updated_at = models.DateTimeField(auto_now=True)
         
         - class Meta:
           - ordering = ['-created_at']
           - indexes = [models.Index(fields=['household', 'created_at'])]
           
         - def __str__(self): return self.title
    2. Add get_absolute_url method returning reverse('recipe_detail', args=[self.pk])
  </action>
  <acceptance_criteria>
    - recipes/models.py contains class Recipe
    - Recipe has household FK, title, description, photo, video_url, on_hand_idea, leftover_worthy, needs_review fields
    - Recipe has db_index on title, on_hand_idea, leftover_worthy, needs_review, created_at
    - Recipe has __str__ method
  </acceptance_criteria>
  <verify>
    <automated>grep -q "class Recipe" recipes/models.py && grep -q "db_index=True" recipes/models.py && echo "PASS"</automated>
  </verify>
  <done>Recipe model created with indexes</done>
</task>

<task type="auto">
  <name>Task 2: Register Recipe in Django admin</name>
  <files>recipes/admin.py</files>
  <read_first>
    - recipes/models.py
  </read_first>
  <action>
    1. Create recipes/admin.py:
       - from django.contrib import admin
       - from .models import Recipe
       
       - @admin.register(Recipe)
         class RecipeAdmin(admin.ModelAdmin):
           - list_display = ['title', 'household', 'needs_review', 'created_at']
           - list_filter = ['needs_review', 'on_hand_idea', 'leftover_worthy', 'created_at']
           - search_fields = ['title', 'description']
           - raw_id_fields = ['household']
  </action>
  <acceptance_criteria>
    - recipes/admin.py exists and registers Recipe
    - RecipeAdmin has list_display, list_filter, search_fields
  </acceptance_criteria>
  <verify>
    <automated>grep -q "@admin.register" recipes/admin.py && echo "PASS"</automated>
  </verify>
  <done>Recipe registered in admin</done>
</task>

<task type="auto">
  <name>Task 3: Create Recipe CRUD views</name>
  <files>recipes/views.py</files>
  <read_first>
    - accounts/views.py (pattern for login-required mixin)
  </read_first>
  <action>
    1. Create recipes/views.py:
       - from django.contrib.auth.mixins import LoginRequiredMixin
       - from django.views.generic import ListView, DetailView, CreateView, UpdateView, DeleteView
       - from django.urls import reverse_lazy
       - from .models import Recipe
       - from .forms import RecipeForm
       
       - class RecipeListView(LoginRequiredMixin, ListView):
         - model = Recipe
         - template_name = 'recipes/recipe_list.html'
         - context_object_name = 'recipes'
         - def get_queryset(self): return Recipe.objects.filter(household=self.request.user.household)
       
       - class RecipeDetailView(LoginRequiredMixin, DetailView):
         - model = Recipe
         - template_name = 'recipes/recipe_detail.html'
         - def get_queryset(self): return Recipe.objects.filter(household=self.request.user.household)
       
       - class RecipeCreateView(LoginRequiredMixin, CreateView):
         - model = Recipe
         - form_class = RecipeForm
         - template_name = 'recipes/recipe_form.html'
         - def form_valid(self, form): form.instance.household = self.request.user.household; return super().form_valid(form)
       
       - class RecipeUpdateView(LoginRequiredMixin, UpdateView):
         - model = Recipe
         - form_class = RecipeForm
         - template_name = 'recipes/recipe_form.html'
         - def get_queryset(self): return Recipe.objects.filter(household=self.request.user.household)
       
       - class RecipeDeleteView(LoginRequiredMixin, DeleteView):
         - model = Recipe
         - template_name = 'recipes/recipe_confirm_delete.html'
         - success_url = reverse_lazy('recipe_list')
         - def get_queryset(self): return Recipe.objects.filter(household=self.request.user.household)
  </action>
  <acceptance_criteria>
    - recipes/views.py contains RecipeListView, RecipeDetailView, RecipeCreateView, RecipeUpdateView, RecipeDeleteView
    - All views inherit LoginRequiredMixin
    - All views filter by household in get_queryset
  </acceptance_criteria>
  <verify>
    <automated>grep -c "LoginRequiredMixin" recipes/views.py</automated>
  </verify>
  <done>Recipe CRUD views created</done>
</task>

<task type="auto">
  <name>Task 4: Create RecipeForm and URLs</name>
  <files>recipes/forms.py, recipes/urls.py</files>
  <read_first>
    - recipes/models.py
  </read_first>
  <action>
    1. Create recipes/forms.py:
       - from django import forms
       - from .models import Recipe
       
       - class RecipeForm(forms.ModelForm):
         - class Meta:
           - model = Recipe
           - fields = ['title', 'description', 'photo', 'video_url', 'on_hand_idea', 'leftover_worthy', 'needs_review']
           - widgets = {
               'description': forms.Textarea(attrs={'rows': 4}),
             }
    
    2. Create recipes/urls.py:
       - from django.urls import path
       - from . import views
       
       - urlpatterns = [
           - path('', views.RecipeListView.as_view(), name='recipe_list'),
           - path('<int:pk>/', views.RecipeDetailView.as_view(), name='recipe_detail'),
           - path('new/', views.RecipeCreateView.as_view(), name='recipe_create'),
           - path('<int:pk>/edit/', views.RecipeUpdateView.as_view(), name='recipe_update'),
           - path('<int:pk>/delete/', views.RecipeDeleteView.as_view(), name='recipe_delete'),
         ]
  </action>
  <acceptance_criteria>
    - recipes/forms.py contains RecipeForm with ModelForm
    - recipes/urls.py contains all 5 route patterns
  </acceptance_criteria>
  <verify>
    <automated>grep -q "class RecipeForm" recipes/forms.py && grep -q "recipe_list" recipes/urls.py && echo "PASS"</automated>
  </verify>
  <done>RecipeForm and URLs created</done>
</task>

<task type="auto">
  <name>Task 5: Create base template and recipe templates</name>
  <files>templates/base.html, templates/recipes/recipe_list.html, templates/recipes/recipe_detail.html, templates/recipes/recipe_form.html, templates/recipes/recipe_confirm_delete.html</files>
  <action>
    1. Create templates/base.html:
       - HTML5 boilerplate
       - Tailwind CSS via CDN for dev: <script src="https://cdn.tailwindcss.com"></script>
       - DaisyUI theme: <script src="https://cdn.jsdelivr.net/npm/daisyui@5"></script>
       - Alpine.js: <script defer src="https://cdn.jsdelivr.net/npm/alpinejs@3.x.x/dist/cdn.min.js"></script>
       - Nav with: Home, Recipes (dropdown), logout link
       - Messages framework display
       - {% block content %}{% endblock %}
    
    2. Create templates/recipes/recipe_list.html:
       - Extends base.html
       - Card grid of recipes showing photo, title, description excerpt
       - "Add Recipe" button
    
    3. Create templates/recipes/recipe_detail.html:
       - Extends base.html
       - Full recipe display: title, photo, description, video_url
       - Edit/Delete buttons
    
    4. Create templates/recipes/recipe_form.html:
       - Extends base.html
       - Django form with crispy or manual Tailwind styling
       - File input for photo
    
    5. Create templates/recipes/recipe_confirm_delete.html:
       - Extends base.html
       - Confirmation form with Delete button
  </action>
  <acceptance_criteria>
    - templates/base.html exists with Tailwind, DaisyUI, Alpine.js
    - templates/recipes/recipe_list.html shows recipe cards
    - templates/recipes/recipe_detail.html shows full recipe
    - templates/recipes/recipe_form.html has form
    - templates/recipes/recipe_confirm_delete.html has confirm
  </acceptance_criteria>
  <verify>
    <automated>ls templates/base.html templates/recipes/*.html 2>/dev/null | wc -l</automated>
  </verify>
  <done>Recipe templates created with Tailwind/DaisyUI styling</done>
</task>

<task type="auto">
  <name>Task 6: Wire URLs and run migrations</name>
  <files>meal_planner/urls.py</files>
  <read_first>
    - meal_planner/urls.py
  </read_first>
  <action>
    1. Add to meal_planner/urls.py:
       - path('', include('recipes.urls', namespace='recipes'))
    
    2. Run migrations:
       - python manage.py makemigrations recipes
       - python manage.py migrate
  </action>
  <acceptance_criteria>
    - recipes/urls included in main urls
    - No migration errors
  </acceptance_criteria>
  <verify>
    <automated>grep -q "recipes.urls" meal_planner/urls.py && echo "PASS"</automated>
  </verify>
  <done>URLs wired and migrations applied</done>
</task>

</tasks>

<verification>
- Navigate to /recipes/ shows recipe list (empty initially)
- Click "Add Recipe" goes to create form
- Submit creates recipe and shows in list
- Click recipe shows detail page
- Edit button goes to edit form
- Delete shows confirmation, deletes on confirm
- All pages styled with Tailwind/DaisyUI
- Data scoped to household
</verification>

<success_criteria>
User can create, view, edit, delete recipes with title, description, photo, video URL, and custom flags. Recipe list displays in card grid. UI styled with Tailwind/DaisyUI.
</success_criteria>

<output>
After completion, create .planning/phases/01-foundation-recipes/01-02-SUMMARY.md
</output>
