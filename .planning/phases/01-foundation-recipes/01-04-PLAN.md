---
phase: 01-foundation-recipes
plan: '04'
type: execute
wave: 2
depends_on:
  - 01-02
  - 01-03
files_modified:
  - recipes/views.py
  - recipes/urls.py
  - templates/recipes/recipe_list.html
autonomous: true
requirements:
  - REC-05
  - REC-06
  - REC-07
  - REC-11
user_setup: []
must_haves:
  truths:
    - User can view recipe list as card/grid
    - User can search recipes by title/description
    - User can sort recipes (rated first, then by rating value, then by date)
    - Recipes with needs_review=True are excluded from normal list
  artifacts:
    - path: recipes/views.py
      provides: RecipeListView with search/sort
    - path: templates/recipes/recipe_list.html
      provides: Card grid with search/sort UI
  key_links:
    - from: RecipeListView.get_context_data
      to: filters
      via: request.GET
      pattern: search\|sort
---

<objective>
Implement recipe search, sorting, and filtering with needs_review exclusion.

Purpose: Allow users to find recipes quickly, sort by rating/date, and ensure recipes needing review don't appear in normal list.

Output: Recipe list with working search, sort dropdown, needs_review excluded.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/research/PITFALLS.md

**Pitfall to avoid:** N+1 queries - use select_related/prefetch_related

**From Plans 01-03:**
- Recipe model exists with all fields
- Rating model exists
- Templates exist with Tailwind/DaisyUI
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update RecipeListView with search and sort</name>
  <files>recipes/views.py</files>
  <read_first>
    - recipes/views.py (existing RecipeListView)
  </read_first>
  <action>
    1. Update RecipeListView to:
       - from django.db.models import Avg, F
       
       - def get_queryset(self):
         - Base: Recipe.objects.filter(household=self.request.user.household, needs_review=False)
         - Add search: if q=self.request.GET.get('q'), filter Q(title__icontains=q) | Q(description__icontains=q)
         - Add sort:
           - 'newest': order_by('-created_at')
           - 'oldest': order_by('created_at')
           - 'rating': annotate(avg_rating=Avg('rating__score')).order_by('-avg_rating')
           - 'title': order_by('title')
         - Add select_related('household') and prefetch_related('tags', 'rating_set')
       
       - def get_context_data(self, **kwargs):
         - Add 'q' from GET param
         - Add 'sort' from GET param
         - Add sort_choices list
  </action>
  <acceptance_criteria>
    - RecipeListView filters by needs_review=False
    - RecipeListView supports q= search param
    - RecipeListView supports sort= param with newest/oldest/rating/title options
  </acceptance_criteria>
  <verify>
    <automated>grep -q "needs_review=False" recipes/views.py && grep -q "get.*q.*" recipes/views.py && echo "PASS"</automated>
  </verify>
  <done>RecipeListView updated with search/sort</done>
</task>

<task type="auto">
  <name>Task 2: Update URL patterns for search/sort</name>
  <files>recipes/urls.py</files>
  <read_first>
    - recipes/urls.py
  </read_first>
  <action>
    1. Keep existing URL patterns - search/sort handled via GET params on same route
  </action>
  <acceptance_criteria>
    - URLs support ?q=search&sort=rating
  </acceptance_criteria>
  <verify>
    <automated>grep -q "recipe_list" recipes/urls.py && echo "PASS"</automated>
  </verify>
  <done>URL patterns support GET params</done>
</task>

<task type="auto">
  <name>Task 3: Update recipe_list template with search bar and sort dropdown</name>
  <files>templates/recipes/recipe_list.html</files>
  <read_first>
    - templates/recipes/recipe_list.html
  </read_first>
  <action>
    1. Add search form at top:
       - <form method="get" action="." class="flex gap-2 mb-4">
       - <input type="text" name="q" placeholder="Search recipes..." value="{{ q|default:'' }}"
       - <select name="sort" class="select select-bordered">
         - Options: newest (default), oldest, rating, title
       - <button type="submit" class="btn btn-primary">Search</button>
    
    2. Update recipe card grid:
       - Show image thumbnail
       - Show title
       - Show rating stars (if rated)
       - Show tags as small badges
       - Show "Needs Review" badge if needs_review=True
    
    3. Add empty state message when no recipes
  </action>
  <acceptance_criteria>
    - Template has search input with name="q"
    - Template has sort select with name="sort"
    - Cards show image, title, rating, tags
    - Empty state shows helpful message
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'name="q"' templates/recipes/recipe_list.html && grep -q 'name="sort"' templates/recipes/recipe_list.html && echo "PASS"</automated>
  </verify>
  <done>Recipe list template updated</done>
</task>

</tasks>

<verification>
- Navigate to /recipes/ shows recipes (excluding needs_review=True)
- Enter search term shows filtered results
- Select "Rating" sorts by highest rated first
- Select "Newest" sorts by date descending
- Recipe cards show thumbnail, title, rating, tags
- Empty list shows helpful message
</verification>

<success_criteria>
User can search recipes by title/description, sort by newest/oldest/rating/title, needs_review recipes excluded from list, card grid displays properly.
</success_criteria>

<output>
After completion, create .planning/phases/01-foundation-recipes/01-04-SUMMARY.md
</output>