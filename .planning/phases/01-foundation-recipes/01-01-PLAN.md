---
phase: 01-foundation-recipes
plan: '01'
type: execute
wave: 1
depends_on: []             # Foundation - runs first
files_modified:
  - meal_planner/settings.py
  - meal_planner/urls.py
  - accounts/models.py
  - accounts/views.py
  - accounts/forms.py
  - accounts/urls.py
  - accounts/templates/registration/login.html
  - accounts/templates/registration/register.html
  - accounts/templates/registration/logged_out.html
  - household/models.py
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-04
  - HOUSE-01
  - HOUSE-02
user_setup: []
must_haves:
  truths:
    - User can register with email/password
    - User can login and stay logged in across sessions
    - User can logout from any page
    - Unauthenticated users are redirected to login
    - User has an associated household record
    - All data operations are scoped to user's household
  artifacts:
    - path: meal_planner/settings.py
      provides: Django project configuration with auth settings
    - path: accounts/models.py
      provides: CustomUser model with household FK
    - path: accounts/views.py
      provides: LoginView, LogoutView, RegisterView
    - path: household/models.py
      provides: Household model
    - path: accounts/templates/registration/login.html
      provides: Login form UI
    - path: accounts/templates/registration/register.html
      provides: Registration form UI
  key_links:
    - from: CustomUser.household
      to: Household.id
      via: ForeignKey
      pattern: household = models.ForeignKey
---

<objective>
Set up Django project foundation with user authentication and household context.

Purpose: Create the foundation for all subsequent phases — authentication gates access, household scoping ensures data isolation between users.

Output: Working login/logout/register flows, Household model, user-household association.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md

**Tech Stack:**
- Django 6.0.3
- PostgreSQL 14+
- psycopg 3.1+

**Key Patterns from Architecture:**
- Household-scoped data isolation via FK
- Service layer pattern for business logic
- Django auth with custom user model
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Django project structure with custom user model</name>
  <files>meal_planner/settings.py, accounts/models.py</files>
  <read_first>
    - meal_planner/settings.py (Django settings structure)
  </read_first>
  <action>
    1. In meal_planner/settings.py, add to INSTALLED_APPS: 'accounts', 'household'
    2. Add AUTH_USER_MODEL = 'accounts.CustomUser' setting
    3. Configure LOGIN_REDIRECT_URL = '/' and LOGOUT_REDIRECT_URL = '/accounts/login/'
    4. Add Message middleware: 'django.contrib.messages.middleware.MessageMiddleware'
    5. In accounts/models.py, create CustomUser model extending AbstractUser with:
       - household = models.ForeignKey('household.Household', on_delete=models.SET_NULL, null=True, blank=True)
    6. Add Meta class with ordering = ['email']
  </action>
  <acceptance_criteria>
    - meal_planner/settings.py contains AUTH_USER_MODEL = 'accounts.CustomUser'
    - meal_planner/settings.py contains 'accounts' and 'household' in INSTALLED_APPS
    - accounts/models.py contains class CustomUser(AbstractUser)
    - CustomUser has household ForeignKey field
  </acceptance_criteria>
  <verify>
    <automated>grep -q "AUTH_USER_MODEL" meal_planner/settings.py && grep -q "class CustomUser" accounts/models.py && echo "PASS"</automated>
  </verify>
  <done>Custom user model created with household FK</done>
</task>

<task type="auto">
  <name>Task 2: Create Household model</name>
  <files>household/models.py</files>
  <read_first>
    - household/models.py (check if exists)
  </read_first>
  <action>
    1. Create household/models.py with Household model containing:
       - name = models.CharField(max_length=100, default="My Household")
       - created_at = models.DateTimeField(auto_now_add=True)
       - updated_at = models.DateTimeField(auto_now=True)
    2. Add Meta class with verbose_name = "Household" and verbose_name_plural = "Households"
    3. Add __str__ method returning self.name
  </action>
  <acceptance_criteria>
    - household/models.py contains class Household
    - Household model has name, created_at, updated_at fields
    - Household has __str__ method
  </acceptance_criteria>
  <verify>
    <automated>grep -q "class Household" household/models.py && echo "PASS"</automated>
  </verify>
  <done>Household model created</done>
</task>

<task type="auto">
  <name>Task 3: Create authentication views and forms</name>
  <files>accounts/views.py, accounts/forms.py, accounts/urls.py</files>
  <read_first>
    - accounts/views.py
    - accounts/forms.py
  </read_first>
  <action>
    1. In accounts/forms.py, create RegistrationForm(UserCreationForm):
       - email = forms.EmailField(required=True)
       - household_name = forms.CharField(max_length=100, required=False)
       - save method creates Household if name provided, associates with user
    2. In accounts/views.py, create RegisterView(FormView):
       - form_class = RegistrationForm
       - template_name = 'registration/register.html'
       - success_url = reverse_lazy('home')
       - form_valid creates user, logs them in, messages success
    3. Add LoginView and LogoutView using Django's built-in with custom templates
    4. In accounts/urls.py, add:
       - path('login/', LoginView.as_view(), name='login')
       - path('logout/', LogoutView.as_view(), name='logout')
       - path('register/', RegisterView.as_view(), name='register')
  </action>
  <acceptance_criteria>
    - accounts/forms.py contains RegistrationForm
    - accounts/views.py contains RegisterView
    - accounts/urls.py contains login, logout, register routes
  </acceptance_criteria>
  <verify>
    <automated>grep -q "class RegistrationForm" accounts/forms.py && grep -q "RegisterView" accounts/views.py && echo "PASS"</automated>
  </verify>
  <done>Authentication views and forms created</done>
</task>

<task type="auto">
  <name>Task 4: Create authentication templates</name>
  <files>accounts/templates/registration/login.html, accounts/templates/registration/register.html, accounts/templates/registration/logged_out.html</files>
  <action>
    1. Create accounts/templates/registration/login.html with:
       - Django login form
       - Tailwind/DaisyUI styling
       - Link to register page
    2. Create accounts/templates/registration/register.html with:
       - RegistrationForm with email, password fields
       - Household name field (optional)
       - Tailwind/DaisyUI styling
    3. Create accounts/templates/registration/logged_out.html with:
       - Success message after logout
       - Link back to login
  </action>
  <acceptance_criteria>
    - login.html exists with form and submit button
    - register.html exists with form and submit button
    - Both use Tailwind CSS classes
  </acceptance_criteria>
  <verify>
    <automated>ls accounts/templates/registration/login.html accounts/templates/registration/register.html 2>/dev/null | wc -l</automated>
  </verify>
  <done>Authentication templates created</done>
</task>

<task type="auto">
  <name>Task 5: Run migrations and verify auth works</name>
  <files></files>
  <action>
    1. Run makemigrations for accounts and household apps:
       python manage.py makemigrations accounts household
    2. Run migrate to create tables:
       python manage.py migrate
    3. Verify user creation works via Django admin or shell
  </action>
  <acceptance_criteria>
    - No migration conflicts
    - Tables created without error
  </acceptance_criteria>
  <verify>
    <automated>python manage.py showmigrations accounts household --plan 2>/dev/null | grep -c "\[X\]" || echo "Migrations need review"</automated>
  </verify>
  <done>Database migrations applied successfully</done>
</task>

</tasks>

<verification>
- User can navigate to /accounts/login/ and see login form
- User can navigate to /accounts/register/ and see registration form
- User can register and is redirected to home
- User can login and session persists
- User can logout and is redirected to login page
- Newly created user has household association
</verification>

<success_criteria>
User can register with email/password, login/logout works, unauthenticated users redirected to login, household model exists with user association.
</success_criteria>

<output>
After completion, create .planning/phases/01-foundation-recipes/01-01-SUMMARY.md
</output>
