---
phase: 02-meal-planning
plan: '04'
type: execute
wave: 1
depends_on: [02-01, 02-02]
files_modified:
  - recipes/models.py
  - meal_planner/views.py
  - meal_planner/urls.py
  - meal_planner/templates/meal_planner/
autonomous: true
requirements:
  - ONHAND-01
  - ONHAND-02
  - ONHAND-03
  - ONHAND-04
  - LEFT-01
  - LEFT-02
  - LEFT-03
user_setup: []
must_haves:
  truths:
    - User can mark recipes as on_hand_idea
    - Modal shows list of on-hand idea recipes
    - User can add/remove recipes from on-hand list
    - User can swap on-hand idea into meal slot
    - User can flag recipes as leftover_worthy
    - Planner loads leftover-worthy meals by date
    - User can plan leftover meals
  artifacts:
    - path: recipes/models.py
      provides: on_hand_idea, leftover_worthy flags on Recipe
    - path: meal_planner/templates/meal_planner/
      provides: On-hand ideas modal
    - path: meal_planner/views.py
      provides: OnHandIdeasView, leftover-aware querysets
  key_links:
    - from: Recipe.on_hand_idea
      to: MealPlan filter
      via: queryset filter
      pattern: filter.*on_hand_idea=True
---

<objective>
Implement on-hand ideas modal and leftover tracking functionality.

Purpose: Quick-access to favorite recipes and leftover meal planning from previously cooked meals.

Output: On-hand ideas modal, leftover flags on recipes, leftover-aware planner.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-meal-planning/02-02-PLAN.md

**Prerequisites:**
- MealPlan CRUD exists
- Recipes exist from Phase 1
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add flags to Recipe model</name>
  <files>recipes/models.py</files>
  <read_first>
    - recipes/models.py (existing Recipe model)
  </read_first>
  <action>
    1. Check if Recipe model already has on_hand_idea and leftover_worthy fields
    2. If not present, add to Recipe model:
       - on_hand_idea = BooleanField(default=False, help_text="Quick access idea")
       - leftover_worthy = BooleanField(default=False, help_text="Good for leftovers")
    3. Add methods:
       - def toggle_on_hand_idea(self): toggle and save
       - def toggle_leftover_worthy(self): toggle and save
  </action>
  <acceptance_criteria>
    - recipes/models.py Recipe has on_hand_idea field
    - recipes/models.py Recipe has leftover_worthy field
  </acceptance_criteria>
  <verify>
    <automated>grep -q "on_hand_idea" recipes/models.py && grep -q "leftover_worthy" recipes/models.py && echo "PASS"</automated>
  </verify>
  <done>Recipe flags added</done>
</task>

<task type="auto">
  <name>Task 2: Create on-hand ideas modal</name>
  <files>meal_planner/templates/meal_planner/on_hand_ideas.html</files>
  <action>
    1. Create templates/meal_planner/on_hand_ideas.html as modal:
       - Modal with title "On-Hand Ideas"
       - Grid/list of recipe cards for on_hand_idea=True recipes
       - Each card: recipe title, rating, tags
       - "Add to meal" button on each card
       - Empty state: "No on-hand ideas yet. Mark recipes as ideas to see them here."
       - Close button
    2. In planner.html add:
       - "On-Hand Ideas" button in header/toolbar
       - Opens modal via htmx or link
  </action>
  <acceptance_criteria>
    - on_hand_ideas.html modal exists
    - Shows list of recipes with on_hand_idea=True
  </acceptance_criteria>
  <verify>
    <automated>ls templates/meal_planner/on_hand_ideas.html 2>/dev/null | wc -l</automated>
  </verify>
  <done>On-hand ideas modal created</done>
</task>

<task type="auto">
  <name>Task 3: Create on-hand ideas views and API</name>
  <files>meal_planner/views.py, meal_planner/urls.py</files>
  <read_first>
    - meal_planner/views.py (existing views)
  </read_first>
  <action>
    1. Create OnHandIdeasView(LoginRequiredMixin, TemplateView):
       - template_name = 'meal_planner/on_hand_ideas.html'
       - get_queryset: Recipe.objects.filter(household=user.household, on_hand_idea=True)
    2. Create ToggleOnHandIdeaView(LoginRequiredMixin, View):
       - POST: toggle on_hand_idea on recipe_id
       - Return JSON {success, on_hand_idea_state}
    3. Create AddOnHandToMealView(LoginRequiredMixin, View):
       - POST: Create MealPlan with recipe from on-hand list
       - Data: meal_date, meal_type, recipe_id
       - Return JSON {success, meal_id}
    4. In urls.py add:
       - path('on-hand/', OnHandIdeasView.as_view(), name='on_hand_ideas')
       - path('api/recipe/<int:recipe_id>/toggle-on-hand/', ToggleOnHandIdeaView.as_view(), name='toggle_on_hand')
       - path('api/on-hand/add-to-meal/', AddOnHandToMealView.as_view(), name='add_on_hand_to_meal')
  </action>
  <acceptance_criteria>
    - OnHandIdeasView returns on-hand recipe list
    - Toggle API works
    - Add-to-meal API works
  </acceptance_criteria>
  <verify>
    <automated>grep -q "OnHandIdeasView" meal_planner/views.py && echo "PASS"</automated>
  </verify>
  <done>On-hand ideas views created</done>
</task>

<task type="auto">
  <name>Task 4: Add on-hand toggle to recipe list/detail</name>
  <files>recipes/templates/recipes/</files>
  <read_first>
    - recipes/templates/recipes/recipe_list.html or recipe_detail.html
  </read_first>
  <action>
    1. Update recipe detail template:
       - Add toggle button for on_hand_idea (star/bookmark icon)
       - Button toggles via ToggleOnHandIdeaView
       - Visual feedback when active (filled icon vs outline)
    2. Update recipe list if needed:
       - Add on_hand_idea toggle in list view
  </action>
  <acceptance_criteria>
    - Recipe detail has on_hand_idea toggle button
    - Button state reflects current value
  </acceptance_criteria>
  <verify>
    <automated>grep -q "on_hand_idea" recipes/templates/recipes/recipe_detail.html 2>/dev/null || echo "Check templates"</automated>
  </verify>
  <done>On-hand toggle UI added</done>
</task>

<task type="auto">
  <name>Task 5: Add leftover tracking to planner</name>
  <files>meal_planner/views.py, meal_planner/templates/meal_planner/planner.html</files>
  <read_first>
    - meal_planner/templates/meal_planner/planner.html
  </read_first>
  <action>
    1. Update planner.html:
       - Add "Show Leftovers" toggle/filter option
       - When active, include recipes where leftover_worthy=True AND cooked recently
    2. Create leftover-aware meal loading:
       - Query: Filter meals where meal_type includes "leftover" or recipe is leftover_worthy
       - Suggest leftover meals based on date
    3. Add leftover_worthy toggle to recipe detail:
       - Same pattern as on_hand_idea
  </action>
  <acceptance_criteria>
    - Planner has leftover filter option
    - Recipe detail has leftover_worthy toggle
  </acceptance_criteria>
  <verify>
    <automated>grep -q "leftover" meal_planner/templates/meal_planner/planner.html && echo "PASS"</automated>
  </verify>
  <done>Leftover tracking integrated</done>
</task>

</tasks>

<verification>
- Recipe detail has star icon for on_hand_idea - click toggles
- On-Hand Ideas button opens modal with recipe list
- Click recipe in modal → add to meal
- Recipe detail has leftover toggle
- Planner can show leftover options
</verification>

<success_criteria>
User can mark recipes as on-hand ideas, view in modal, add to meal. User can mark recipes as leftover-worthy and see in planner.
</success_criteria>

<output>
After completion, create .planning/phases/02-meal-planning/02-04-SUMMARY.md
</output>