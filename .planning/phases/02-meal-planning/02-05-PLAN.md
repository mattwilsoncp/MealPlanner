---
phase: 02-meal-planning
plan: '05'
type: execute
wave: 2
depends_on: [02-01, 02-02, 02-03, 02-04]
files_modified:
  - meal_planner/views.py
  - meal_planner/urls.py
  - meal_planner/templates/meal_planner/
  - inventory/models.py
autonomous: true
requirements:
  - COOK-01
  - COOK-02
  - COOK-03
  - COOK-04
  - COOK-05
  - COOK-06
  - COOK-07
  - COOK-08
user_setup: []
must_haves:
  truths:
    - User can initiate cooking from a meal card
    - Reconciliation page shows recipe ingredients (left) and inventory (right)
    - Inventory separated into "Still Have" and "Used / Ran Out"
    - User can check off recipe ingredients visually
    - User can move inventory items between sections
    - User can mark all inventory as used
    - User can add recipe ingredient to inventory inline
    - Confirm button processes used items and updates inventory
  artifacts:
    - path: meal_planner/views.py
      provides: CookingReconciliationView, ProcessCookingView
    - path: meal_planner/templates/meal_planner/cooking.html
      provides: Two-column reconciliation UI
    - path: meal_planner/urls.py
      provides: /cooking/ routes
  key_links:
    - from: CookingReconciliationView.queryset
      to: Recipe.ingredients + Inventory
      via: queryset filter
      pattern: recipe\.ingredients\.all.*inventory
---

<objective>
Implement cooking reconciliation functionality.

Purpose: Track inventory usage when cooking, mark ingredients as used, and update inventory counts.

Output: Working reconciliation page where users can track what was used while cooking.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-meal-planning/02-04-PLAN.md

**Prerequisites:**
- All prior meal planning plans complete
- Recipes and ingredients exist (Phase 1)
- Inventory exists (will link, may need to create minimal inventory model)
- Side dishes exist (Plan 03)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create cooking reconciliation view</name>
  <files>meal_planner/views.py</files>
  <read_first>
    - meal_planner/views.py (existing views)
  </read_first>
  <action>
    1. Create CookingHomeView(LoginRequiredMixin, ListView):
       - model = MealPlan
       - template_name = 'meal_planner/cooking_home.html'
       - queryset = meals where meal_date is today/yesterday OR marked "cook today"
       - Filter by household
    2. Create CookingReconciliationView(LoginRequiredMixin, DetailView):
       - model = MealPlan
       - template_name = 'meal_planner/cooking.html'
       - Get recipe.ingredients.all
       - Get household inventory items
       - Context: recipe_ingredients, inventory_items, meal_id
    3. Create json_reconciliation_data view:
       - Returns ingredients and inventory for a meal
       - Return: {ingredients: [{id, name, quantity, unit}], inventory: [{id, name, quantity, unit}]}
  </action>
  <acceptance_criteria>
    - CookingHomeView shows meals ready to cook
    - CookingReconciliationView shows ingredients vs inventory
  </acceptance_criteria>
  <verify>
    <automated>grep -q "CookingReconciliationView" meal_planner/views.py && echo "PASS"</automated>
  </verify>
  <done>Cooking views created</done>
</task>

<task type="auto">
  <name>Task 2: Create cooking URLs</name>
  <files>meal_planner/urls.py</files>
  <read_first>
    - meal_planner/urls.py
  </read_first>
  <action>
    1. In urls.py add:
       - path('cooking/', CookingHomeView.as_view(), name='cooking_home')
       - path('cooking/<int:meal_id>/', CookingReconciliationView.as_view(), name='cooking_reconcile')
       - path('api/cooking/<int:meal_id>/data/', json_reconciliation_data, name='api_cooking_data')
  </action>
  <acceptance_criteria>
    - /cooking/ shows cooking queue
    - /cooking/123/ shows reconciliation
  </acceptance_criteria>
  <verify>
    <automated>grep -q "cooking" meal_planner/urls.py && echo "PASS"</automated>
  </verify>
  <done>Cooking URLs created</done>
</task>

<task type="auto">
  <name>Task 3: Create reconciliation template (two-column)</name>
  <files>meal_planner/templates/meal_planner/cooking.html</files>
  <action>
    1. Create cooking.html:
       - Header: "Cooking: [Recipe Title]" with meal date
       - Two-column layout:
         Left: Recipe ingredients list with checkboxes
         Right: Inventory items divided into "Still Have" and "Used / Ran Out" sections
       - Each ingredient has: name, quantity, unit, checkbox to mark as used
       - Each inventory item: name, quantity, unit, checkbox (default in "Still Have")
       - "Move to Used" button for inventory items
       - "Mark All Used" button
       - "Add Ingredient to Inventory" button (inline add form)
       - "Confirm Cooking" button at bottom
       - Tailwind/DaisyUI styling
  </action>
  <acceptance_criteria>
    - cooking.html shows two columns
    - Left: recipe ingredients
    - Right: inventory with sections
  </acceptance_criteria>
  <verify>
    <automated>ls templates/meal_planner/cooking.html 2>/dev/null | wc -l</automated>
  </verify>
  <done>Reconciliation template created</done>
</task>

<task type="auto">
  <name>Task 4: Create process cooking API view</name>
  <files>meal_planner/views.py</files>
  <read_first>
    - meal_planner/views.py
  </read_first>
  <action>
    1. Create ProcessCookingView(LoginRequiredMixin, View):
       - POST: take meal_id, used_ingredient_ids[], updated_inventory_ids[]
       - For each used ingredient:
         - If has linked inventory: decrement inventory quantity
         - If no link: option to add to inventory
       - Mark meal as "cooked" (add cooked_at timestamp)
       - Update inventory in "Used" section
       - Return JSON {success, updated_inventory}
    2. Create mark_ingredient_used view:
       - AJAX endpoint to toggle ingredient usage
       - Returns current state
  </action>
  <acceptance_criteria>
    - ProcessCookingView updates inventory
    - Returns success JSON
  </acceptance_criteria>
  <verify>
    <automated>grep -q "ProcessCookingView" meal_planner/views.py && echo "PASS"</automated>
  </verify>
  <done>Process cooking API created</done>
</task>

<task type="auto">
  <name>Task 5: Add cooking initiation from planner</name>
  <files>meal_planner/templates/meal_planner/planner.html</files>
  <read_first>
    - meal_planner/templates/meal_planner/planner.html
  </read_first>
  <action>
    1. Update planner.html:
       - Add "Cook This" button on each meal card
       - Links to cooking_reconcile with meal_id
       - Optional: mark meal as "Cook Today" from planner
    2. Update cooking_home.html:
       - List meals by date
       - "Start Cooking" button on each
  </action>
  <acceptance_criteria>
    - Meal cards have "Cook This" button
    - Links to reconciliation page
  </acceptance_criteria>
  <verify>
    <automated>grep -q "Cook" meal_planner/templates/meal_planner/planner.html | head -3</automated>
  </verify>
  <done>Cooking initiation integrated</done>
</task>

</tasks>

<verification>
- Click "Cook This" on meal card → reconciliation page
- On reconciliation: show ingredients left, inventory right
- Check off ingredients as used
- Move inventory between sections
- Click "Confirm" → inventory updated
</verification>

<success_criteria>
User can initiate cooking from meal card, reconcile ingredients with inventory, track what was used.
</success_criteria>

<output>
After completion, create .planning/phases/02-meal-planning/02-05-SUMMARY.md
</output>