---
phase: 02-meal-planning
plan: '01'
type: execute
wave: 1
depends_on: []             # Phase 1 provides foundation, recipes already exist
files_modified:
  - meal_planner/models.py
  - meal_planner/urls.py
  - meal_planner/views.py
  - meal_planner/admin.py
autonomous: true
requirements:
  - MEAL-01
  - MEAL-02
  - MEAL-03
  - MEAL-04
  - MEAL-06
user_setup: []
must_haves:
  truths:
    - Weekly planner shows 7 days with meal type sections
    - User can navigate between weeks
    - Meal types = breakfast, lunch, dinner, snack
    - User can add multiple meals per type per day
    - User can add custom/free-text meals (not recipe-linked)
  artifacts:
    - path: meal_planner/models.py
      provides: MealPlan, MealType models
    - path: meal_planner/views.py
      provides: WeeklyPlannerView, WeekNavigationView
    - path: meal_planner/templates/meal_planner/planner.html
      provides: Weekly planner UI with 7-day grid
  key_links:
    - from: MealPlan.meal_date
      to: Week dates
      via: Date selection
      pattern: meal_date__range
    - from: MealPlan.recipe
      to: Recipe.id
      via: ForeignKey
      pattern: recipe = models.ForeignKey
---

<objective>
Create meal planning models and weekly planner view.

Purpose: Core meal planning functionality — the 7-day weekly view with navigation and basic meal type organization.

Output: MealPlan model, weekly planner template with 7-day grid, navigation between weeks.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

**Phase 1 Status:** Complete - recipe model exists
**Phase 2 Dependencies:**
- Requires Phase 1 (recipes must exist to link to meals)

**Tech Stack (from Phase 1):**
- Django 6.0.3
- Tailwind CSS 4.x + DaisyUI 5.x
- django-bootstrap-icons for icons
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create MealPlan and MealType models</name>
  <files>meal_planner/models.py</files>
  <read_first>
    - meal_planner/models.py (existing from Phase 1 - may have Recipe model already)
  </read_first>
  <action>
    1. In meal_planner/models.py, create MealType choices:
       class MealType(models.TextChoices):
           BREAKFAST = 'breakfast', 'Breakfast'
           LUNCH = 'lunch', 'Lunch'
           DINNER = 'dinner', 'Dinner'
           SNACK = 'snack', 'Snack'
    2. Create MealPlan model:
       - household = ForeignKey to Household
       - meal_date = DateField
       - meal_type = CharField with MealType choices
       - recipe = ForeignKey to Recipe (nullable=True, blank=True)
       - custom_meal = CharField max_length=200 (nullable=True, blank=True)
       - notes = TextField (nullable=True, blank=True)
       - meal_rating = IntegerField (nullable=True) # 1-5 scale
       - created_at = DateTimeField(auto_now_add=True)
       - updated_at = DateTimeField(auto_now=True)
       - Meta: unique_together = [household, meal_date, meal_type, recipe], ordering = [meal_date, meal_type]
    3. Add property: is_custom (True if recipe is None)
    4. Add __str__ method returning f"{meal_date} - {meal_type} - {recipe or custom_meal}"
  </action>
  <acceptance_criteria>
    - meal_planner/models.py contains class MealType(TextChoices)
    - meal_planner/models.py contains class MealPlan
    - MealPlan has household, meal_date, meal_type, recipe, custom_meal, notes, meal_rating fields
    - MealPlan has unique_together constraint
  </acceptance_criteria>
  <verify>
    <automated>grep -q "class MealType" meal_planner/models.py && grep -q "class MealPlan" meal_planner/models.py && echo "PASS"</automated>
  </verify>
  <done>MealPlan and MealType models created</done>
</task>

<task type="auto">
  <name>Task 2: Create meal planner URLs and views</name>
  <files>meal_planner/urls.py, meal_planner/views.py</files>
  <read_first>
    - meal_planner/urls.py (existing URL patterns)
    - meal_planner/views.py (existing views pattern)
  </read_first>
  <action>
    1. In meal_planner/views.py, create PlannerHomeView:
       - LoginRequiredMixin
       - TemplateView with template planner.html
       - Get current week (Monday-Sunday) from URL params or default to current week
       - Context: start_date, end_date, week_days list [(date, day_name)]
    2. Create week_navigate view:
       - Redirect to planner with week offset (+1 or -1 from current week_date)
    3. Create json_week_meals API:
       - GET: return meals for date range as JSON
       - Query: MealPlan.objects.filter(household=user.household, meal_date__gte=start, meal_date__lte=end)
       - Return: [{id, meal_date, meal_type, recipe_id, recipe_title, custom_meal, notes, meal_rating}]
    4. In meal_planner/urls.py, add:
       - path('planner/', PlannerHomeView.as_view(), name='planner')
       - path('planner/<int:year>/<int:week>/', PlannerHomeView.as_view(), name='planner_week')
       - path('planner/navigate/', week_navigate, name='planner_navigate')
       - path('api/meals/', json_week_meals, name='api_meals')
  </action>
  <acceptance_criteria>
    - meal_planner/urls.py contains planner routes
    - meal_planner/views.py contains PlannerHomeView
    - URL names: planner, planner_navigate, api_meals exist
  </acceptance_criteria>
  <verify>
    <automated>grep -q "PlannerHomeView" meal_planner/views.py && grep -q "planner" meal_planner/urls.py && echo "PASS"</automated>
  </verify>
  <done>Meal planner URLs and views created</done>
</task>

<task type="auto">
  <name>Task 3: Create weekly planner template</name>
  <files>meal_planner/templates/meal_planner/planner.html</files>
  <action>
    1. Create templates/meal_planner/planner.html with:
       - Week header with prev/next navigation arrows
       - Date range display: "Week of Jan 1 - Jan 7, 2026"
       - 7-column grid for days (use CSS grid)
       - Each day column has sections for: Breakfast, Lunch, Dinner, Snack
       - Each meal type section shows meal cards
       - "Add Meal" button for each meal type
       - Empty state: "No meals planned"
       - Tailwind/DaisyUI styling, dark mode
    2. Add meal card display:
       - If recipe: show recipe title (linkable)
       - If custom_meal: show custom text
       - Notes preview if present
  </action>
  <acceptance_criteria>
    - planner.html exists in templates/meal_planner/
    - Shows 7 days with navigation
    - Has meal type sections: Breakfast, Lunch, Dinner, Snack
    - Uses Tailwind CSS classes
  </acceptance_criteria>
  <verify>
    <automated>ls templates/meal_planner/planner.html 2>/dev/null | wc -l</automated>
  </verify>
  <done>Weekly planner template created</done>
</task>

<task type="auto">
  <name>Task 4: Run migrations and verify models</name>
  <files></files>
  <action>
    1. Run makemigrations:
       python manage.py makemigrations meal_planner
    2. Run migrate:
       python manage.py migrate
    3. Register MealPlan in admin.py
  </action>
  <acceptance_criteria>
    - No migration conflicts
    - MealPlan table created
  </acceptance_criteria>
  <verify>
    <automated>python manage.py showmigrations meal_planner --plan 2>/dev/null | grep -c "\[X\]" || echo "Migrations need review"</automated>
  </verify>
  <done>Database migration applied</done>
</task>

</tasks>

<verification>
- Navigate to /planner/ �� see 7-day grid
- Navigate to /planner/2026/4/ — see specific week
- Prev/Next week links work
- Each day shows meal type sections
</verification>

<success_criteria>
Weekly planner shows 7 days, navigation works, user can view meal types for any date range.
</success_criteria>

<output>
After completion, create .planning/phases/02-meal-planning/02-01-SUMMARY.md
</output>