---
phase: 02-meal-planning
plan: '03'
type: execute
wave: 1
depends_on: [02-01, 02-02]
files_modified:
  - meal_planner/models.py
  - meal_planner/views.py
  - meal_planner/urls.py
  - meal_planner/templates/meal_planner/planner.html
autonomous: true
requirements:
  - MEAL-05
  - MEAL-10
  - MEAL-11
user_setup: []
must_haves:
  truths:
    - User can link a recipe to a meal slot
    - User can view linked recipe from meal card
    - User can add side dishes to a meal (linked to recipe or custom text)
  artifacts:
    - path: meal_planner/models.py
      provides: SideDish model (linked to MealPlan)
    - path: meal_planner/views.py
      provides: Recipe selection API endpoint
    - path: meal_planner/templates/meal_planner/planner.html
      provides: Recipe link, recipe detail modal, side dish UI
  key_links:
    - from: MealPlan.recipe
      to: Recipe.id
      via: ForeignKey
      pattern: recipe = models.ForeignKey\(Recipe
    - from: SideDish.meal_plan
      to: MealPlan.id
      via: ForeignKey
      pattern: meal_plan = models.ForeignKey
---

<objective>
Add recipe linking and side dishes functionality.

Purpose: Connect meal plan entries to existing recipes and support side dishes.

Output: Recipe selector in add/edit forms, recipe detail view from meal card, side dish support.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-meal-planning/02-01-PLAN.md
@.planning/phases/02-meal-planning/02-02-PLAN.md

**Prerequisites:**
- MealPlan model exists with recipe field
- Add/edit views exist
- Meal form template exists
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SideDish model</name>
  <files>meal_planner/models.py</files>
  <read_first>
    - meal_planner/models.py (existing MealPlan model)
  </read_first>
  <action>
    1. In meal_planner/models.py, add SideDish model:
       class SideDish(models.Model):
           meal_plan = ForeignKey(MealPlan, on_delete=models.CASCADE, related_name='side_dishes')
           recipe = ForeignKey(Recipe, on_delete=models.SET_NULL, null=True, blank=True, related_name='as_side_dish')
           custom_side = CharField(max_length=200, null=True, blank=True)
           order = PositiveIntegerField(default=0)
           notes = TextField(blank=True)
           class Meta:
               ordering = ['order']
           def __str__(self):
               return self.recipe.name if self.recipe else self.custom_side
    2. Add related_name='side_dishes' to MealPlan if not present
  </action>
  <acceptance_criteria>
    - meal_planner/models.py contains class SideDish
    - SideDish has meal_plan, recipe, custom_side, order fields
  </acceptance_criteria>
  <verify>
    <automated>grep -q "class SideDish" meal_planner/models.py && echo "PASS"</automated>
  </verify>
  <done>SideDish model created</done>
</task>

<task type="auto">
  <name>Task 2: Add recipe selection to meal form</name>
  <files>meal_planner/forms.py, meal_planner/views.py</files>
  <read_first>
    - meal_planner/forms.py (MealPlanForm)
  </read_first>
  <action>
    1. Update MealPlanForm to filter recipe queryset:
       - queryset = Recipe.objects.filter(household=user.household, needs_review=False).order_by('title')
       - Override __init__ to accept user and filter queryset
    2. Create SideDishForm(ModelForm):
       - model = SideDish
       - fields = [recipe, custom_side, order, notes]
    3. Create SideDishInlineFormSet:
       - modelform_factory(SideDish, form=SideDishForm, extra=1)
       - Can add multiple side dishes
  </action>
  <acceptance_criteria>
    - MealPlanForm filters recipes by household
    - SideDishForm exists
  </acceptance_criteria>
  <verify>
    <automated>grep -q "SideDishForm" meal_planner/forms.py && echo "PASS"</automated>
  </verify>
  <done>Side dish form created</done>
</task>

<task type="auto">
  <name>Task 3: Create recipe selection API endpoint</name>
  <files>meal_planner/views.py, meal_planner/urls.py</files>
  <read_first>
    - meal_planner/views.py (existing API views)
  </read_first>
  <action>
    1. Create RecipeSelectView(LoginRequiredMixin, View):
       - GET: return JSON list of user's recipes for selection dropdown
       - Query: Recipe.objects.filter(household=user.household, needs_review=False).values('id', 'title', 'rating')
       - Return: [{id, title, rating}]
    2. Create RecipeDetailView(LoginRequiredMixin, DetailView):
       - model = Recipe
       - template_name = 'meal_planner/recipe_detail_inline.html'
       - Return JSON for modal display
    3. In urls.py add:
       - path('api/recipes/select/', RecipeSelectView.as_view(), name='api_recipe_select')
       - path('api/recipes/<int:pk>/', RecipeDetailView.as_view(), name='api_recipe_detail')
  </action>
  <acceptance_criteria>
    - RecipeSelectView returns JSON recipe list
    - RecipeDetailView returns recipe detail
  </acceptance_criteria>
  <verify>
    <automated>grep -q "RecipeSelectView" meal_planner/views.py && echo "PASS"</automated>
  </verify>
  <done>Recipe API endpoints created</done>
</task>

<task type="auto">
  <name>Task 4: Update meal form to include side dishes</name>
  <files>meal_planner/templates/meal_planner/meal_form.html</files>
  <read_first>
    - meal_planner/templates/meal_planner/meal_form.html (existing)
  </read_first>
  <action>
    1. Update meal_form.html:
       - Add "Side Dishes" section below main meal
       - Link to Recipe selector or enter custom side dish text
       - "Add another side" button adds formset entry
       - Reorderable side dishes (drag handle or up/down arrows)
    2. Add JavaScript:
       - Load recipe list via RecipeSelectView
       - Populate recipe dropdown with search
       - Show recipe preview when recipe selected
  </action>
  <acceptance_criteria>
    - meal_form.html has side dish section
    - Can add multiple side dishes (recipe or custom)
  </acceptance_criteria>
  <verify>
    <automated>grep -q "SideDish\|side_dish\|side dish" meal_planner/templates/meal_planner/meal_form.html && echo "PASS"</automated>
  </verify>
  <done>Side dish UI integrated</done>
</task>

<task type="auto">
  <name>Task 5: Add recipe link and detail modal to planner</name>
  <files>meal_planner/templates/meal_planner/planner.html</files>
  <read_first>
    - meal_planner/templates/meal_planner/planner.html
  </read_first>
  <action>
    1. Update planner.html meal cards:
       - If recipe linked: show recipe title as clickable link
       - Click opens modal with recipe details (ingredients, instructions)
    2. Add modal for recipe detail:
       - Recipe title, description
       - Ingredient list
       - Instructions preview
       - Link to full recipe page
    3. Show side dishes on meal cards:
       - "with [side1], [side2]" format below main meal
  </action>
  <acceptance_criteria>
    - Recipe title is clickable
    - Click shows recipe detail modal
    - Side dishes display on meal cards
  </acceptance_criteria>
  <verify>
    <automated>grep -q "recipe" meal_planner/templates/meal_planner/planner.html | head -5</automated>
  </verify>
  <done>Recipe linking complete</done>
</task>

</tasks>

<verification>
- Add meal → select recipe from dropdown → recipe saved
- Click recipe title in meal card → modal shows recipe details
- Add side dish → appears on meal card
- Custom side dishes also work
</verification>

<success_criteria>
User can link recipe to meal, view recipe from meal card, add side dishes (recipe or custom).
</success_criteria>

<output>
After completion, create .planning/phases/02-meal-planning/02-03-SUMMARY.md
</output>