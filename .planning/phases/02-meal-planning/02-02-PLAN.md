---
phase: 02-meal-planning
plan: '02'
type: execute
wave: 1
depends_on: [02-01]
files_modified:
  - meal_planner/forms.py
  - meal_planner/views.py
  - meal_planner/urls.py
  - meal_planner/templates/meal_planner/
autonomous: true
requirements:
  - MEAL-04
  - MEAL-07
  - MEAL-08
  - MEAL-09
user_setup: []
must_haves:
  truths:
    - User can add multiple meals per type per day
    - User can update a meal (change recipe or notes)
    - User can delete a meal
    - User can rate a meal plan entry
  artifacts:
    - path: meal_planner/views.py
      provides: AddMealView, EditMealView, DeleteMealView
    - path: meal_planner/forms.py
      provides: MealPlanForm
    - path: meal_planner/templates/meal_planner/
      provides: Add/edit meal modal or page
  key_links:
    - from: AddMealView.form_valid
      to: MealPlan.objects.create
      via: form.save()
      pattern: form_valid.*save
---

<objective>
Add meal CRUD operations — add, edit, delete, and rate meals.

Purpose: Users need to manage their weekly meals — create new meal entries, modify existing ones, and remove planned meals they no longer want.

Output: Working add/edit/delete flows for meals in the weekly planner.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-meal-planning/02-01-PLAN.md
@.planning/ROADMAP.md

**Prerequisites:**
- MealPlan model created (Plan 01)
- Weekly planner view exists (Plan 01)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create MealPlanForm for add/edit</name>
  <files>meal_planner/forms.py</files>
  <read_first>
    - meal_planner/forms.py (if exists from Phase 1)
  </read_first>
  <action>
    1. Create MealPlanForm(ModelForm):
       - model = MealPlan
       - fields = [meal_date, meal_type, recipe, custom_meal, notes]
       - meal_type as Select widget with MealType choices
       - recipe as Select with queryset filtered to user's recipes
       - custom_meal hidden via JavaScript when recipe selected
    2. Add clean method:
       - Require either recipe or custom_meal (not both empty)
  </action>
  <acceptance_criteria>
    - meal_planner/forms.py contains MealPlanForm
    - Form has meal_date, meal_type, recipe, custom_meal fields
  </acceptance_criteria>
  <verify>
    <automated>grep -q "class MealPlanForm" meal_planner/forms.py && echo "PASS"</automated>
  </verify>
  <done>MealPlanForm created</done>
</task>

<task type="auto">
  <name>Task 2: Create add/edit/delete views</name>
  <files>meal_planner/views.py, meal_planner/urls.py</files>
  <read_first>
    - meal_planner/views.py (existing views from Plan 01)
  </read_first>
  <action>
    1. Create AddMealView(LoginRequiredMixin, CreateView):
       - model = MealPlan
       - form_class = MealPlanForm
       - template_name = 'meal_planner/meal_form.html'
       - success_url = reverse_lazy('planner')
       - form_valid: set household to user.household before save
    2. Create EditMealView(LoginRequiredMixin, UpdateView):
       - same as AddMealView
       -pk_url_kwarg = 'meal_id'
    3. Create DeleteMealView(LoginRequiredMixin, DeleteView):
       - model = MealPlan
       - success_url = reverse_lazy('planner')
       - post: only delete on POST with confirm
    4. Create RateMealView(LoginRequiredMixin, View):
       - POST: take meal_id and rating (1-5), update meal_rating
       - Return JSON response
    5. In meal_planner/urls.py add:
       - path('meal/add/', AddMealView.as_view(), name='add_meal')
       - path('meal/<int:meal_id>/edit/', EditMealView.as_view(), name='edit_meal')
       - path('meal/<int:meal_id>/delete/', DeleteMealView.as_view(), name='delete_meal')
       - path('meal/<int:meal_id>/rate/', RateMealView.as_view(), name='rate_meal')
  </action>
  <acceptance_criteria>
    - views.py contains AddMealView, EditMealView, DeleteMealView, RateMealView
    - urls.py contains add_meal, edit_meal, delete_meal, rate_meal routes
  </acceptance_criteria>
  <verify>
    <automated>grep -q "AddMealView" meal_planner/views.py && grep -q "add_meal" meal_planner/urls.py && echo "PASS"</automated>
  </verify>
  <done>Meal CRUD views created</done>
</task>

<task type="auto">
  <name>Task 3: Create meal form template with modal support</name>
  <files>meal_planner/templates/meal_planner/meal_form.html</files>
  <action>
    1. Create templates/meal_planner/meal_form.html:
       - Form with date picker, meal type dropdown
       - Recipe dropdown (queryset from view)
       - "Or enter custom meal" text input (shows when no recipe selected)
       - Notes textarea (optional)
       - Submit button, Cancel link
       - Tailwind/DaisyUI styling, dark mode
    2. Add modal wrapper for inline use:
       - Modal dialog with form inside
       - JavaScript to show/hide custom meal field based on recipe selection
  </action>
  <acceptance_criteria>
    - meal_form.html exists with form fields
    - Has recipe dropdown, custom_meal field, meal_type selection
  </acceptance_criteria>
  <verify>
    <automated>ls templates/meal_planner/meal_form.html 2>/dev/null | wc -l</automated>
  </verify>
  <done>Meal form template created</done>
</task>

<task type="auto">
  <name>Task 4: Integrate add/edit into planner view</name>
  <files>meal_planner/templates/meal_planner/planner.html</files>
  <read_first>
    - meal_planner/templates/meal_planner/planner.html (existing from Plan 01)
  </read_first>
  <action>
    1. Update planner.html:
       - "Add meal" button in each meal type section
       - When clicked, opens modal or links to add_meal?date=YYYY-MM-DD&type=breakfast
       - Each meal card has Edit/Delete icons
       - Edit links to edit_meal with meal_id
       - Delete shows confirmation before POST
       - Star/ rating icon for each meal card
    2. Add inline form rendering for adding meals without page navigation:
       - Modal dialog with MealPlanForm
       - AJAX submit returns JSON, updates UI
  </action>
  <acceptance_criteria>
    - planner.html has Add buttons for each meal type
    - Meals have Edit/Delete actions
    - Rating functionality integrated
  </acceptance_criteria>
  <verify>
    <automated>grep -q "add_meal" meal_planner/templates/meal_planner/planner.html && echo "PASS"</automated>
  </verify>
  <done>Add/edit integrated into planner</done>
</task>

</tasks>

<verification>
- Click "Add meal" → form appears
- Submit adds meal to database
- Click Edit → form pre-filled with meal data
- Click Delete → meal removed after confirm
- Click rating → meal_rating updated
</verification>

<success_criteria>
User can add multiple meals per day, edit existing meals, delete meals, and rate meal plan entries.
</success_criteria>

<output>
After completion, create .planning/phases/02-meal-planning/02-02-SUMMARY.md
</output>