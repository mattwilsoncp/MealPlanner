---
phase: 04-discovery-matching
plan: '01'
type: execute
wave: 1
depends_on: []
files_modified:
  - shopping/services.py
  - shopping/tests/test_matching.py
autonomous: true
requirements:
  - MATCH-01
  - MATCH-02
  - MATCH-03
  - MATCH-05
  - MATCH-06
must_haves:
  truths:
    - "Matching data includes available vs missing ingredients for each recipe"
    - "Results include urgency flags tied to household expiration rules"
    - "Result set can be deterministically sorted by urgency and match percentage"
  artifacts:
    - path: shopping/services.py
      provides: Recipe-match aggregation and ordering service for discovery UI
    - path: shopping/tests/test_matching.py
      provides: Regression tests for sorting, missing ingredients, and urgency detection
  key_links:
    - from: shopping/services.py
      to: inventory.models.InventoryItem
      via: household-scoped inventory query and expiration-date evaluation
      pattern: InventoryItem\.objects\.filter\(household=
    - from: shopping/services.py
      to: ingredients.models.IngredientLink
      via: ingredient requirement extraction per recipe
      pattern: IngredientLink\.objects\.filter\(recipe=
---

<objective>
Build deterministic matching service outputs that power Discovery UI sorting, missing badges, and urgent-expiration surfacing.

Purpose: Deliver the core recipe scoring contract before view/template wiring so downstream rendering is implementation-only.
Output: Tested service API returning per-recipe match details, missing ingredients, and urgency metadata.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-discovery-matching/04-RESEARCH.md
@shopping/services.py
@shopping/tests/test_generation.py
@inventory/models.py
@household/models.py

<interfaces>
From shopping/services.py:
```python
def compute_meal_match(recipe, inventory_items):
    return {
        "available_count": int,
        "total_count": int,
        "match_percentage": float,
    }
```

From inventory/models.py:
```python
class InventoryItem(models.Model):
    household = models.ForeignKey(Household, ...)
    name = models.CharField(max_length=200)
    quantity = models.DecimalField(max_digits=8, decimal_places=2)
    expiration_date = models.DateField(null=True, blank=True)
```

From household/models.py:
```python
class Household(models.Model):
    expiring_threshold_days = models.PositiveIntegerField(default=7)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing tests for discovery-match service contract</name>
  <files>shopping/tests/test_matching.py</files>
  <read_first>
    - shopping/tests/test_generation.py
    - shopping/services.py
    - inventory/models.py
  </read_first>
  <behavior>
    - Service returns missing ingredient names for partially matched recipes.
    - Service marks urgency when matched inventory has expiration_date <= today + household.expiring_threshold_days.
    - Service ordering is urgent-first then match_percentage descending.
  </behavior>
  <action>Create `shopping/tests/test_matching.py` with tests for a new service function `build_discovery_matches(household, as_of_date=None)` that returns list entries containing: `recipe`, `available_count`, `total_count`, `match_percentage`, `missing_ingredients` (list[str]), `urgent_items` (list[str]), `has_expired_match` (bool), and `has_urgent_match` (bool). Include fixtures for at least three recipes (high match/no urgent, medium match/urgent, low match/no urgent) and assert stable ordering: urgent recipe appears before non-urgent even when its percentage is lower; among same urgency, higher percentage sorts first.</action>
  <acceptance_criteria>
    - shopping/tests/test_matching.py contains tests for missing_ingredients, urgent flags, and deterministic ordering
    - tests assert presence of keys: missing_ingredients, urgent_items, has_expired_match, has_urgent_match
    - test file imports build_discovery_matches from shopping.services
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python manage.py test shopping.tests.test_matching -v 2</automated>
  </verify>
  <done>Service contract is executable and test-defined before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement discovery-match aggregation and ordering logic</name>
  <files>shopping/services.py, shopping/tests/test_matching.py</files>
  <read_first>
    - shopping/services.py
    - ingredients/models.py
    - inventory/models.py
    - household/models.py
  </read_first>
  <behavior>
    - Only positive-quantity inventory counts as available.
    - Missing ingredient badges derive from ingredient names not present in available inventory.
    - Urgent and expired flags derive from expiration_date and household threshold.
  </behavior>
  <action>In `shopping/services.py`, implement `build_discovery_matches(household, as_of_date=None)` using household-scoped recipe, ingredient-link, and inventory queries. Reuse normalized-name matching conventions already used in `_normalized_key`. For each recipe, compute counts via `compute_meal_match`, populate missing ingredient names, detect `urgent_items` where inventory expiration_date is within threshold window, and set `has_expired_match` when expiration_date &lt; as_of_date. Return a sorted list with key `(not has_urgent_match, -match_percentage, recipe.title.casefold())` so urgent items always surface first and ties are stable.</action>
  <acceptance_criteria>
    - shopping/services.py exports build_discovery_matches
    - implementation performs household-scoped ORM filtering for recipes and inventory
    - returned match dict includes missing and urgency fields defined by tests
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python manage.py test shopping.tests.test_matching shopping.tests.test_generation -v 2</automated>
  </verify>
  <done>MATCH-01/02 baseline is extended to support MATCH-03/05/06 data requirements for UI rendering.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Authenticated request context → discovery match service | User identity and household scope determine data visibility |
| Inventory expiration metadata → ranking logic | Data quality issues can alter urgency-driven recommendation ordering |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | I | build_discovery_matches household reads | mitigate | Scope every recipe/inventory query to `household` and cover cross-household leakage with test fixtures |
| T-04-02 | T | urgency ranking inputs | mitigate | Derive urgency strictly from server-side `expiration_date` and `expiring_threshold_days`; ignore client-supplied values |
| T-04-03 | D | matching service query fanout | mitigate | Use `select_related`/batched lookups and avoid per-recipe DB queries inside loops |
</threat_model>

<verification>
- `.venv/bin/python manage.py test shopping.tests.test_matching shopping.tests.test_generation -v 2`
- `.venv/bin/python manage.py check`
</verification>

<success_criteria>
- Service contract returns sortable match dataset including missing ingredients and urgency metadata.
- Deterministic ordering supports urgent-first + percentage sorting for discovery UI.
- Automated tests lock behavior against regression.
</success_criteria>

<output>
After completion, create `.planning/phases/04-discovery-matching/04-01-SUMMARY.md`
</output>
