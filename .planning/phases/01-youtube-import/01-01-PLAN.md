---
phase: 01-youtube-import
plan: '01'
type: execute
wave: 1
depends_on: []             # First phase - no dependencies
files_modified:
  - recipes/views.py
  - recipes/urls.py
  - recipes/forms.py
  - templates/recipes/import.html
autonomous: true
requirements:
  - IMP-01
  - IMP-02
  - IMP-03
  - IMP-04
user_setup: []
must_haves:
  truths:
    - User can navigate to /recipes/import/ to access import page
    - User can paste a YouTube URL (youtube.com or youtu.be) into input field
    - URL is validated before making API call
    - Loading spinner displays while import is in progress
    - Invalid URL shows user-friendly error message inline
  artifacts:
    - path: recipes/views.py
      provides: RecipeImportView class for handling import page
    - path: recipes/urls.py
      provides: Route for /recipes/import/
    - path: recipes/forms.py
      provides: ImportForm with YouTube URL validation
    - path: templates/recipes/import.html
      provides: Import page UI with URL input, loading state, error display
---

<objective>
Create YouTube URL import page where users can paste a YouTube URL, validate it, and initiate import with loading feedback.

Purpose: First step in YouTube recipe import flow — gives users a dedicated import page to paste URLs and see loading/error states.

Output: Working import page at /recipes/import/ with URL input, validation, loading spinner, and error messaging.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-youtube-import/01-CONTEXT.md
@.planning/v1.1-ROADMAP.md
@.planning/REQUIREMENTS.md

**Tech Stack:**
- Django 6.0.3
- PostgreSQL 14+
- Tailwind CSS / DaisyUI (existing styling)

**Key Decisions (from CONTEXT.md):**
- D-01: Dedicated import page at /recipes/import/
- D-02: Allow any URL, let API fail naturally (but validate youtube.com or youtu.be pattern first)
- D-03: Spinner button for loading state
- D-05: On success, redirect to recipe form pre-populated (later phase)
- D-06: Error messages display inline on the import page

**Existing Patterns:**
- RecipeForm in recipes/forms.py — for reference on form styling
- RecipeListView pattern — for import view structure
- Dark theme: .input-dark, .btn-pill classes in base.html
- Django messages framework for inline errors
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add import URL route</name>
  <files>recipes/urls.py</files>
  <read_first>
    - recipes/urls.py
  </read_first>
  <action>
    1. Add import route to recipes/urls.py:
       - path('import/', views.RecipeImportView.as_view(), name='recipe_import')
    2. Place it after the list view and before the detail view for logical grouping
  </action>
  <acceptance_criteria>
    - recipes/urls.py contains path('import/', ...) with name='recipe_import'
  </acceptance_criteria>
  <verify>
    <automated>grep -q "recipe_import" recipes/urls.py && echo "PASS"</automated>
  </verify>
  <done>Import URL route added to urls.py</done>
</task>

<task type="auto">
  <name>Task 2: Create ImportForm with YouTube URL validation</name>
  <files>recipes/forms.py</files>
  <read_first>
    - recipes/forms.py
  </read_first>
  <action>
    1. Add ImportForm class to recipes/forms.py:
       - youtube_url = forms.URLField with required=True
       - Clean method validates youtube.com or youtu.be domain
       - Error message: "Please enter a valid YouTube URL (youtube.com or youtu.be)"
    2. Use existing .input-dark class styling from RecipeForm
  </action>
  <acceptance_criteria>
    - recipes/forms.py contains class ImportForm
    - ImportForm has youtube_url field with URL validation
    - ImportForm validates youtube.com or youtu.be domain
  </acceptance_criteria>
  <verify>
    <automated>grep -q "class ImportForm" recipes/forms.py && grep -q "youtube.com\|youtu.be" recipes/forms.py && echo "PASS"</automated>
  </verify>
  <done>ImportForm created with YouTube URL validation</done>
</task>

<task type="auto">
  <name>Task 3: Create RecipeImportView</name>
  <files>recipes/views.py</files>
  <read_first>
    - recipes/views.py
  </read_first>
  <action>
    1. Add RecipeImportView class to recipes/views.py:
       - LoginRequiredMixin for authentication
       - FormView with ImportForm
       - template_name = 'recipes/import.html'
       - form_valid currently shows success message and redirects (later phases handle actual import)
       - Use Django messages framework for inline errors
    2. Follow the same pattern as RecipeListView for class structure
  </action>
  <acceptance_criteria>
    - recipes/views.py contains class RecipeImportView
    - RecipeImportView uses LoginRequiredMixin
    - RecipeImportView renders import.html template
  </acceptance_criteria>
  <verify>
    <automated>grep -q "class RecipeImportView" recipes/views.py && grep -q "import.html" recipes/views.py && echo "PASS"</automated>
  </verify>
  <done>RecipeImportView created in views.py</done>
</task>

<task type="auto">
  <name>Task 4: Create import template with loading state</name>
  <files>templates/recipes/import.html</files>
  <read_first>
    - templates/base.html
    - templates/recipes/recipe_form.html
  </read_first>
  <action>
    1. Create templates/recipes/import.html:
       - Extends base.html
       - Contains form with YouTube URL input field
       - Submit button that shows spinner while form is submitting
       - Error display area using Django messages
       - Clean, focused "wizard" style UI
    2. Use existing .input-dark and .btn-pill classes for consistency
    3. Include JavaScript to show loading spinner on form submit
  </action>
  <acceptance_criteria>
    - templates/recipes/import.html exists
    - Template contains URL input field with name="youtube_url"
    - Template contains submit button with loading state
    - Template displays form errors inline
  </acceptance_criteria>
  <verify>
    <automated>test -f templates/recipes/import.html && grep -q "youtube_url" templates/recipes/import.html && echo "PASS"</automated>
  </verify>
  <done>Import template created with loading state</done>
</task>

<task type="auto">
  <name>Task 5: Add navigation link to import page</name>
  <files>templates/recipes/recipe_list.html</files>
  <read_first>
    - templates/recipes/recipe_list.html
  </read_first>
  <action>
    1. Add "Import from YouTube" link/button to recipe_list.html:
       - Place near the "New Recipe" button
       - Links to /recipes/import/
    2. Use consistent button styling with existing "New Recipe" button
  </action>
  <acceptance_criteria>
    - recipe_list.html contains link to recipe_import view
    - Link is visible and accessible from recipe list page
  </acceptance_criteria>
  <verify>
    <automated>grep -q "recipe_import" templates/recipes/recipe_list.html && echo "PASS"</automated>
  </verify>
  <done>Navigation link to import page added</done>
</task>

</tasks>

<verification>
- User can navigate to /recipes/import/ and see import form
- Invalid URL (not youtube.com or youtu.be) shows validation error
- Empty URL shows required field error
- Valid YouTube URL passes validation and shows success message (placeholder for now)
- Loading spinner appears when form is submitted
- User can navigate back to recipe list from import page
</verification>

<success_criteria>
User can enter a YouTube URL in a dedicated import field, system validates URL format (youtube.com or youtu.be) before processing, import button shows loading state while fetching is in progress, invalid URL shows clear user-friendly error message.
</success_criteria>

<output>
After completion, create .planning/phases/01-youtube-import/01-01-SUMMARY.md
</output>
