---
phase: 05-phase1-gap-remediation
plan: '01'
type: execute
wave: 1
depends_on: []
files_modified:
  - reviews/tests/test_review_queue.py
  - recipes/tests/test_recipe_editing.py
  - reviews/views.py
  - reviews/urls.py
  - recipes/forms.py
  - templates/reviews/review_queue.html
  - templates/recipes/recipe_form.html
autonomous: true
requirements:
  - REC-12
  - INST-02
  - TAG-02
must_haves:
  truths:
    - User can open a review queue page that lists recipes where needs_review is true
    - User can reorder instruction steps while editing a recipe and save the new order
    - User can create a new tag inline while editing a recipe and assign it immediately
  artifacts:
    - path: reviews/views.py
      provides: Household-scoped review queue query and context for needs-review recipes
    - path: recipes/forms.py
      provides: Recipe edit form handling for instruction ordering and inline tag creation
    - path: templates/recipes/recipe_form.html
      provides: UI controls for instruction reordering and new-tag creation
  key_links:
    - from: templates/recipes/recipe_form.html
      to: recipes/forms.py
      via: posted instruction order and new-tag fields
      pattern: name="instruction_.*_order|new_tag_name"
    - from: reviews/views.py
      to: templates/reviews/review_queue.html
      via: context variable carrying needs-review queryset
      pattern: needs_review
---

<objective>
Close recipe/review workflow requirement gaps for review queue visibility, instruction reorder persistence, and inline tag creation.

Purpose: These three behaviors were flagged unsatisfied in the milestone audit and block Phase 1 closure.
Output: Tested recipe/review flow updates covering REC-12, INST-02, and TAG-02.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/v1-MILESTONE-AUDIT.md
@.planning/phases/01-foundation-recipes/01-03-SUMMARY.md
@.planning/phases/01-foundation-recipes/01-05-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing regression tests for REC-12, INST-02, and TAG-02</name>
  <files>reviews/tests/test_review_queue.py, recipes/tests/test_recipe_editing.py</files>
  <behavior>
    - Review queue endpoint returns household-scoped recipes where `needs_review=True`.
    - Recipe edit POST can submit reordered instruction steps and persists order deterministically.
    - Recipe edit POST can create a new household tag and attach it to the recipe in one save.
  </behavior>
  <action>Create/extend tests so they fail against current behavior: review queue coverage in `reviews/tests/test_review_queue.py`; recipe edit behavior coverage in `recipes/tests/test_recipe_editing.py` for instruction reorder and inline tag creation. Include cross-household fixture data to ensure no leakage in review queue results.</action>
  <verify>
    <automated>.venv/bin/python manage.py test reviews.tests.test_review_queue recipes.tests.test_recipe_editing -v 2</automated>
  </verify>
  <done>Failing tests clearly encode REC-12, INST-02, and TAG-02 acceptance behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement review queue and recipe-edit backend support</name>
  <files>reviews/views.py, reviews/urls.py, recipes/forms.py</files>
  <behavior>
    - Review queue always filters by `request.user.household` and `needs_review=True`.
    - Instruction reorder input is validated and stored as contiguous, ascending step positions.
    - New tag names are normalized and household-scoped before attach.
  </behavior>
  <action>Update `reviews/views.py` and `reviews/urls.py` so review queue behavior is explicit and test-covered for household scope (REC-12). In `recipes/forms.py`, add recipe-edit handling for posted instruction order and inline `new_tag_name` creation/assignment, keeping existing tag assignment behavior intact (INST-02, TAG-02). Reject empty/duplicate new tag names within household.</action>
  <verify>
    <automated>.venv/bin/python manage.py test reviews.tests.test_review_queue recipes.tests.test_recipe_editing -v 2</automated>
  </verify>
  <done>All Task 1 tests pass with backend logic enforcing scope, reorder persistence, and tag creation.</done>
</task>

<task type="auto">
  <name>Task 3: Wire recipe and review templates to new backend fields</name>
  <files>templates/reviews/review_queue.html, templates/recipes/recipe_form.html</files>
  <action>Update `templates/reviews/review_queue.html` to render the tested review queue context without placeholder data. Update `templates/recipes/recipe_form.html` to expose instruction ordering controls and an inline new-tag input that posts `new_tag_name` with existing form submission. Keep Tailwind/DaisyUI patterns used in Phase 1 templates.</action>
  <verify>
    <automated>.venv/bin/python manage.py test reviews.tests.test_review_queue recipes.tests.test_recipe_editing -v 2 && .venv/bin/python manage.py check</automated>
  </verify>
  <done>UI controls map directly to backend fields and all regression tests remain green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser form input → recipe edit handlers | Untrusted reorder/tag input can mutate recipe metadata |
| Authenticated request → review queue query | User identity boundary controls household data visibility |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | I | reviews/views.py queue query | mitigate | Enforce `request.user.household` filter in queryset and test cross-household exclusion. |
| T-05-02 | T | recipes/forms.py reorder payload | mitigate | Validate instruction order inputs as integers and normalize contiguous ordering before save. |
| T-05-03 | E | inline tag creation | mitigate | Create tags only within authenticated household and dedupe by normalized name per household. |
</threat_model>

<verification>
- `.venv/bin/python manage.py test reviews.tests.test_review_queue recipes.tests.test_recipe_editing -v 2`
- `.venv/bin/python manage.py check`
</verification>

<success_criteria>
- REC-12 behavior is test-verified and household-scoped.
- INST-02 reorder behavior persists and displays in saved order.
- TAG-02 inline creation works in recipe edit flow with duplicate protection.
</success_criteria>

<output>
After completion, create `.planning/phases/05-phase1-gap-remediation/05-01-SUMMARY.md`
</output>
